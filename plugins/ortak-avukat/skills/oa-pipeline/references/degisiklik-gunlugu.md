# oa-pipeline — Değişiklik Günlüğü

(SKILL.md gövdesinden bağlam ekonomisi için taşındı; içerik aynen korunmuştur.)

- **2026-06 (v1):** İlk sürüm. Dokuz adımlı sabit hat + dinamik mod + kritik kavşak protokolü + durum takibi.
- **2026-06 (v1.1):** Denetim güncellemesi (Can yönlendirmesi): (1) **Layer 0 katmanı** bağlandı — hattın her adımındaki dış araç çağrısı `oa-gizlilik` süzgecinden geçer (gizlilik bir adım değil, sarmalayan katmandır). (2) **10. KAPANIŞ** adımı eklendi — dosya sonuçlanınca `oa-arsiv`'e ders kaydı (arşivin yazma tarafı; salt okuma tarafı 3. adımda zaten vardı) + tekrarlayan iş tipinde `oa-usta` tetiği. Döngü kapandı: sistem dosya işleyen değil, dosyadan öğrenen.
- **2026-06:** Anayasal düstur işlendi (Can yönlendirmesi): **usul esasa üstündür** — süre usul hukukunun parçasıdır; düstur bu parçanın işlevine operatif kuralla bağlandı (yukarıdaki bölüm). Müvekkil menfaati çift yönlü: kendi usul zaafını sıfırla, karşı tarafın usul hatasını (özellikle kaçırılmış süreyi) tespit et ve derhâl kullan.
- **2026-06 (v1.2):** **`oa-usul` katmanı** omurgaya bağlandı (Can yönlendirmesi): usulün esasa takaddümü hattın her adımını sarar — usul soruları/içtihadı/denetimi her aşamada öncelikli; karşı taraf usul eksiği anında işlenir, müvekkil hatasında kapı araştırması açılır.
- **2026-06:** Örnekleme ilkesi bağlandı (anayasal — Can yönlendirmesi): konu sayımları örneklemdir, kapsam tüm Türk hukukudur; işlemeyen örneklem güncellenir, metod sabittir.
- **2026-06:** ZORUNLU TAM TUR protokolü eklendi (Can yönlendirmesi): tetiklendiğimde 17 parçanın disiplinini istisnasız işletirim; her parça UYGULANDI/GEREKSİZ/BİLGİ-EKSİK olarak işaretlenir (sessiz atlama yok); akış sonunda BAŞBAKAN katman kontrol listesi (her parça statüsü + öz-denetim teyidi: kestirme yapılmadı, script'ler gerçekten koştu); script'li parçalarda gerçek script çalışır (determinizm), erişilemezse bildirilir; kalıcı katmanlar gereksiz işaretlenemez.
- **2026-06:** Çaba/kalite standardı bağlandı (anayasal — Can yönlendirmesi): tasarruf hedef değil; derinlik karmaşıklığa göre yükselir; Opus+High taban.
- **2026-06:** Doğaçlama meşruiyeti bağlandı (anayasal — Can yönlendirmesi): yöntemde serbest doğaçlama (Çapar lafzı), olguda sıfır halüsinasyon/teyit.
- **2026-06:** BAŞBAKAN kimliği eklendi (Can yönlendirmesi): oa-pipeline anayasanın icra+denetim organı; istisnasız işletim (ama/fakat yok), her aşamada tembellik/kestirme öz-denetimi, görevden kaçış yasağı + dürüst "yapılamadı" beyanı + alternatif yöntem üretimi zorunlu.
- **2026-06:** Başbakan→Çırak besleme bağı netleştirildi: KAPANIŞ adımı oa-usta'nın (Çırak) sürekli öğrenme kaynağıdır; pipeline akışı gözlemlenip derse damıtılır.
- **2026-06:** İki kural eklendi (Can yönlendirmesi): (4) ANAYASAYA UYGUNLUK — Başbakan tüm düsturlara uygun çalışmak zorunda, her aşamada anayasa-uygunluk öz-denetimi, aykırılık teslim engeli; (5) MCP AKTİFLİĞİ ZORUNLU — iş başında gerekli MCP'lerin aktifliği doğrulanır, kapalıysa körlemesine devam yok (dur+uyar), doğrulanmamış olgu teyitliymiş gibi sunulamaz, aktif edilemezse "MCP teyidi beklemede" etiketi. (MCP'yi açmak Cowork araç ayarında; skill açamaz ama kapalıyken doğrulama atlanamaz.)
- **2026-06:** HALÜSİNASYON DENETİM YETKİSİ eklendi (Can yönlendirmesi): Başbakan, oa-ictihat/oa-illiyet dahil tüm parçaların ürettiği künye (Yargıtay/Danıştay/BAM/AYM) ve mevzuat maddelerini AYRICA Yargı Pro/Mevzuat MCP teyidinden geçirir; teyitsiz/uydurma unsur DOĞRUDAN DIŞLANIR; teftiş ARAŞTIRMA/OLGU/KIYAS/KONTROL adımlarında tekrarlanır (künye sapması denetimi).
- **2026-06:** Tutarlılık düzeltmesi (Can denetim talebi): ZORUNLU TAM TUR kuralı 1'de "17 parça" → "ailenin tüm parçaları (18 oa- parçası) + 0. MANİFEST" olarak gerçek aile durumuyla hizalandı (v3.13 entegrasyonunda gözden kaçan maddi hata).
- **2026-07 (v3.16):** FİZİKSEL İŞLETİM PROTOKOLÜ eklendi (Can yönlendirmesi — komutla tetiklemede parçalar description'dan simüle ediliyordu): parça = fiilî Skill çağrısı + gerçek script + gerçek MCP çağrısı; `scripts/pipeline_kayit.py` defteri kuruldu (UYGULANDI kanıtsız yazılamaz, `--denetle` boşluklu turu hata koduyla reddeder — Başbakan denetiminin deterministik motoru); kademeli yükleme + DEVİR PAKETİ + OTONOM MOD (subagent) tanımlandı; 0. MANİFEST'e `scripts/manifest_olustur.py` deterministik sayım scripti bağlandı; durum takibi ikili kaynağa (JSON makine-doğrusu + md insan-görünümü) ayrıldı.
- **2026-07 (v3.17):** ÇALIŞMA KÖKÜ VE YEREL HAFIZA bölümü + `scripts/oa_hafiza.py` (init / devir / teyit / ajan-brif / oturum / durum — Can yönlendirmesi: hafıza ve devir çalışılan klasörde fiziksel yaşasın): devir paketleri ve KÜNYE TEYİT KÜTÜĞÜ fiziksel dosya oldu (kütükte olmayan künye çıktıya 'teyitli' giremez); defter varsayılan yolu `_oa/defter/pipeline-durum.json`; OTONOM MOD'a deterministik alt-ajan brifi (`ajan-brif`) bağlandı; YAZIM adımına `oa-sozlesme` dalı eklendi; parça sayısı 19'a güncellendi.
- **2026-07 (v3.18):** Rapor §5 önerileri uygulandı (Can yönlendirmesi): ÇALIŞMA EVRAKI KURALI (NN-parca-icerik adlandırması; evraksız adım UYGULANDI olamaz) + TEK OTURUM KURALI ve kilit (`oturum-ac`/`oturum-kapat`, ritüelsiz kapanış script tarafından REDDedilir) + `oa_hafiza.py` v2 (sure-flag/sureler.json, oturum kilidi, devralma sırası çıktısı) + `scripts/udf_metin.py` (UYAP UDF→metin; 'okunamadı' ancak script başarısızsa) + Codex dahil ortam bağımsızlığı vurgusu.
- **2026-07 (v3.19):** Saha dersleri: (1) SCRIPT KEŞFİ + ARAÇ ÇANTASI — scriptler skill kökü/. claude yolundan hat başında `_oa/araclar/`a kopyalanır; bulunamazsa ELDEN-MCP-teyitli mod baş+son beyanla (istisna, standart değil). (2) ELDEN MOD md-defter standardı. (3) Adım kanıtının rapor metnine gömülmesi evrak sayılmaz. Kök neden notu: Capabilities'e SKILL.md-yapıştırma kurulumu bundled dosyaları taşımaz — kurulum .skill/zip ile yapılmalı.
- **2026-07:** Çaba/token düsturu GÜNCELLENDİ (Can yönlendirmesi): tasarruf artık HEDEF — ama yalnız mekanik/temsil katmanında ve VERİ-KAYIPSIZ; muhakemede tasarruf edilmez, derinlik/doğrulama/araştırma asla kısılmaz. Aile geneli anayasal güncelleme; deterministik motor: `oa-ingest`.
- **2026-07 (Aşama 4 — oa-ingest v1.5 paralel entegrasyonu):** `oa_ingest.py`'nin v1.5 `--isci` paralelliği (varsayılan otomatik `min(çekirdek,8)`; `--isci 1` seri) 0. MANİFEST hattına dokümante bağlandı: Sabit hat 0. adımı ve EVRAK ATLAMA YASAĞI/büyük dosya protokolü artık `oa_ingest.py` çağrısını `--isci` notuyla açıkça anıyor; SUBAGENT ORKESTRASYONU'ndaki fan-out maddesine iki paralellik katmanının (alt-ajan fan-out ↔ oa-ingest'in kendi süreç havuzu) bağımsız olduğu netliği eklendi. TAM TUR/DELTA akışı (`tam_tur.py`) ve `manifest_olustur.py --mutabakat` mekanik kapısı değiştirilmedi — `--isci` yalnız duvar-saatini kısaltır, künye/md çıktısı seri koşuyla BYTE-ÖZDEŞ kalır (bkz. `oa-ingest` v1.5 determinizm kanıtı). Entegrasyon testi: `tests/test_pipeline_ingest_entegrasyon.py`.
- **2026-07 (v3.21 — M1-3, GATE B okuma kapısı, Denizli canlı testinden):** Yeni `scripts/okuma_kapisi.py` (`--kok`, cwd-bağımsız): `_oa/metin/00-INDEX.md`/`00-kunye.json` (+ Gate A haritaları) üzerinden bir alt-ajana SADECE MEKANİK "öncelikli evrak listesi + büyük-evrak uyarısı" üretir — `tur_tahmini`ne (Gate C) uygulanan SABİT bir sıra tablosuyla diz(t)er, muhakeme yapmaz. İkinci işlev: TAM-YÜKLEME DEDUP DEFTERİ (`_oa/defter/tam-yukleme.jsonl`, append-only, `pipeline_kayit.py` olay-defteri deseniyle atomik) — `--tam-yukle-kaydet "<kaynak>"` ile bir büyük evrağın TAM yüklendiği loglanır; aynı evrak 2. kez tam yüklenirse script yalnız UYARIR, BLOKLAMAZ (advisory — derinlik kısılmaz). `oa_hafiza.py ajan-brif` çıktısına "OKUMA DİSİPLİNİ" maddesi (yeni 4, eskiler 5/6/7'ye kaydı) bağlandı: önce 00-INDEX + büyük-evrak haritaları, büyük evrağı haritadan oku, gerçekten gerekiyorsa tam yükle + deftere logla. Testler: `tests/test_okuma_kapisi.py`, `tests/test_oa_hafiza.py` (OKUMA DİSİPLİNİ ek doğrulaması).
- **2026-07 (v3.22 — M1-4, GATE D+E, Denizli canlı testinden — Okuma Ekonomisi ölçüm/delta paketi):** (D) `oa_metrik.py` genişledi: yeni [5] ANALİZ TOKEN RAPORU bölümü `_oa/defter/tam-yukleme.jsonl`'i (Gate B'nin TAM-YÜKLEME DEDUP DEFTERİ — kaynak+ajan+zaman) 00-kunye.json karakter alanlarıyla eşleyip HER ALT-AJANIN tam yüklediği evrak-token'ını + toplamı derler (`ajan_bazinda`, `toplam_token`); eşik (varsayılan: külliyat `tahmini_token`, kunye yoksa sabit `ANALIZ_ESIK_VARSAYILAN`; `--analiz-esik-token` ile elle) aşılırsa "SEÇİCİ OKU" UYARISI basılır — bu bir ÖLÇÜM uyarısıdır (kapı değil, `oa_metrik` her koşulda exit 0 döner); kaynak künyede yoksa (elle girilmiş/silinmiş) o olay `kaynak_bulunamadi` sayaçlı ayrı tutulur, sessizce yutulmaz/uydurulmaz. (E) `tam_tur.py`'ye TAM TUR DELTA ZORLAMA'yı doğrudan tüketilebilir bir talimat metnine çeviren `--brif` komutu eklendi (Gate G + delta hesabına DAYANIR, `--durum` ile AYNI mekanik sinyal — ayrı bir gerçek icat edilmedi): "TAM AÇIK" (tam tur güncel + delta yok) → ajanı HAM evrağı toplu yeniden OKUMAMASI / tam turu TEKRAR KOŞMAMASI yönünde açıkça talimatlandırır (yalnız dosya-analiz.md + GELİŞMELER okunur); "KISMİ" (bekleyen delta var) → yalnız o evrak(lar)ı tek tek işlemeye yönlendirir; "KAPALI" → normal ZORUNLU TAM TUR'a döner. SKILL.md'ye "ARTIMLI MOD KAPISI — Gate E" bölümü ve KAPANIŞ adımına Gate D notu bağlandı; ARAŞTIRMA/analiz adımı artık sabit hat başlamadan `--brif`i okur. Testler: `tests/test_oa_metrik.py` (5 yeni: yok-damgası, ajan-bazında toplama, eşik-aşımı uyarısı, elle eşik, bilinmeyen-kaynak ayrımı), `tests/test_tam_tur.py` (4 yeni: kapalı/tam-açık/kısmi/durum-ile-aynı-kod).
- **2026-07 (v3.23 — M3-0, DOĞUM-ANI KALICILIK, Gate G+ — kullanıcı şartı: dosya-analiz.md her dosyada BAŞTAN kurulsun, DAİMA mevcut, kayıpsız):** `tam_tur.py` TEK SAHİP kalarak (oa_ingest/oa_hafiza bu dosyaya YAZMAZ) üç komutu TEK render motoru (`_md_render`) üzerine birleştirdi: **`--baslat`** dosya-analiz.md YOKSA 15 bölümlük (`<!-- oa:bolum:NN-ad -->`, 0-Künye … 14-Diğer Çalışma Evrakları CATCH-ALL) bir İSKELETİ atomik doğurur, VARSA idempotent (gövdeye dokunmaz, yalnız dosya-adı değiştiyse başlığı tazeler); **YENİ `--senkron`** md'yi İSKELET + `_oa/cikti/*` + `00-kunye.json` + `dosya-analiz.json`'dan DETERMİNİSTİK yeniden türetir (küçük çıktı TAM gömülü, büyük çıktı öz+yol+sha16; `_oa/cikti/NN-*` adlandırması NN=00-09 → bölüm NN+1 ile eşlenir, eşlenmeyen HER dosya bölüm 14'e düşer — hiçbir çalışma evrakı kaybolmaz); **`--kaydet`** artık "son `--senkron` + evrak snapshot'ı + `<!-- oa:tam-tur:TAMAM <tarih> -->` damgası" olarak yeniden kuruldu (`--ekle`'nin doğrudan md-append'i KALKTI — GELİŞMELER artık yalnız durum.json'da yaşar, bir sonraki senkron/kaydet'te md'ye yansır). **Gate G+:** eski Gate G'nin üç koşulu (VAR+dolu+taze) İSKELETİ de geçtiği için dördüncü bir koşul eklendi — md içinde YALNIZ `--kaydet`in yazdığı TAMAM işaretçisi VAR mı; işaretçisiz iskelet HER ZAMAN "tamamlanmadi" sayılır (fail-closed). Kendini-onarma: `--durum`/`--senkron` md yoksa/bozuksa (bölüm ayraçları eksikse) onu birincil kaynaklardan yeniden kurar + GÖRÜNÜR uyarı basar; bu onarım TAMAM ÜRETMEZ. Şerh (--zorla) artık durum.json `serh_tarihcesi`'nde KALICI — md yeniden türetilse bile bölüm 13'te görünmeye devam eder. SKILL.md 0. MANİFEST sırasına `tam_tur --baslat`/`--senkron` bağlandı; TAM TUR YAŞAM DÖNGÜSÜ'ne DOĞUM-ANI KALICILIK paragrafı eklendi. `oa_hafiza.py ajan-brif` OKUMA DİSİPLİNİ maddesine dosya-analiz.md'nin ucuz devir giriş noktası olduğu tek satırla bağlandı; `oa-ingest` SKILL.md'ye `tam_tur --senkron` notu eklendi. Testler: `tests/test_tam_tur.py` (7 yeni: iskelet-doğumu+idempotentlik, başlık-tazeleme, senkron-kayıpsız-türetim+catch-all, Gate G+ işaretçisiz-iskelet, senkron-kendini-onarma, şerh-kalıcılığı; mevcut 2 test self-heal davranışına göre güncellendi, geri kalan 11 test AYNEN korundu).
- **2026-07 (v3.24 — M3-0 DÜZELTME, denetçi bulgusu — ANAYASAL İHLAL onarımı, gizli özetleme kaldırıldı):** v3.23'ün `_cikti_govde` eşiği (`KUCUK_ESIK=4000` karakter) TÜM bölümlere ayrımsız uygulanıyordu — 4-Araştırma+İçtihat Muhakeme, 6-Kıyas, 7-Strateji, 8-Antitez, 9-Yazım gibi SAF MUHAKEME bölümlerinde 4000 karakteri aşan (tipik: birkaç bin kelimelik gerekçelendirme) bir çalışma evrakı dosya-analiz.md'ye yalnız ilk ~300 karakterlik bir 'Öz' + yol + sha16 ile giriyordu; ARTIMLI MOD (yalnız dosya-analiz.md + GELİŞMELER okunur, ham `_oa/cikti` TEKRAR AÇILMAZ) altında sonraki oturum bu özetle ilerliyordu — anayasa §1 ('muhakemede... tasarruf ASLA yapılmaz') ve hard invaryant ('MUHAKEME KAYBI YOK', 'ÖZETLEME/digest YASAK') ihlaliydi. **Düzeltme:** `KUCUK_ESIK` eşiği ve boyut kapısı TAMAMEN KALDIRILDI — `_cikti_govde` artık OKUNABİLİR (utf-8 çözülebilir) her `_oa/cikti/*` dosyasını BOYUTTAN BAĞIMSIZ TAM gömer; yalnız GERÇEKTEN İKİLİ/okunamayan içerik (zaten düzyazı muhakeme taşımaz — metne çevrilebilseydi oa-ingest onu markdown olarak üretirdi) öz+yol+sha16 ile temsil edilmeye devam eder. Yan bulgu (tasarım netliği, ihlal değil): `--senkron`, `--kaydet` sonrası (TAMAM damgalı) bir md üzerinde çağrılırsa TAMAM işaretçisini sessizce düşürüyordu (`tamam_tarih=None` render'ı) — veri kaybı yoktu ama şaşırtıcıydı; artık `cmd_senkron` bu düşüşü AÇIKÇA stderr'e bildirir ve `cmd_baslat`'ın "ara güncelleme için: --senkron" mesajı bu davranışı netleştirir. Testler: `tests/test_tam_tur.py` (3 yeni: büyük-muhakeme-çıktısı-özetlenmeden-tam-gömülür ("Öz:" deseni ARTIK üretilmiyor doğrulaması dahil), ikili-içerik-hâlâ-öz-temsille-gösterilir, senkron-kaydet-sonrası-TAMAM-düşüşü-uyarısı; mevcut 19 test AYNEN korundu — toplam 22).
- **2026-07 (v3.25 — M3-3):** (R4) 5.KIYAS adım açıklamasındaki "İLLİYET" alanı **DAVAYA-BAĞ** oldu (`oa-kiyas/references/ictihat-muhakeme-sablonu.md` ile hizalı). (R2) 9.KONTROL adımındaki "DÖRT yeşil ışık" sayma dili kaldırıldı; tek ölçüt artık `teslim_paketi.py`'nin exit kodu — alt kapıları elle sayma açıkça yasaklandı. (R5) GATE B — OKUMA KAPISI açıklamasına açık sınır cümlesi eklendi: Gate B yalnız aynı büyük evrağın gereksiz 2. tam yüklemesini (İSRAF) engeller, seçici/derin okumayı ASLA kısmaz (anayasa §1).
- **2026-07 (v3.26 — M3-4 hizalama):** Sürüm işaretçisi ailenin M3 faz-sonu ortak hizalama sürümüne (v3.26) taşındı (`aile_dogrula.py` sürüm tutarlılık uyarısını kapatmak için); bu satırın kendisi dışında bu parçada işlevsel bir değişiklik YOKTUR — gerçek içerik değişiklikleri (varsa) yukarıdaki ayrı kayıtlardadır.
- **2026-07-28 (v0.5.5 — Paket A/B/C/D, aktivasyon çekirdeği + zorlama zinciri + ekonomi + muhakeme):** Bu koşuşun merkez sahibi olarak dört paket birden buraya bağlandı: **P0-4** Gate G döngü kırıcı (`tam_tur.py` in-process import, subprocess sınıfı kalktı); **P0-5** TESLİM MAKBUZU (`teslim-makbuz.json`, fail-closed); **P0-6** kademeli önkoşul-artefakt kapısı; **P0-7** MODEL-BAĞIMSIZ TETİK (Stop/SessionEnd hook → `pipeline_kayit.py --denetle` + `oa_metrik.py`); **P0-8** DURUM.md fiziksel-kanıt türetimi; **P1-8** KAPANIŞ Gate (RET akışı kilitlemez); **P1-9** gölge-hat çözümü (`--onbakis` ayrı artefakt, TEK-YAZAR bekçisi); **P1-10** canlı-senkron kapısı; **P1-12/13** UTF-8 guard + regresyon/ölçüm sayaçları; **M1** DAVA TEZİ+PAS PROTOKOLÜ, **M4** unsur şablon bağı, **M7** AVUKAT KARARI çözüm komutu. Yeni scriptler `oa_hafiza.py`/`oa_metrik.py`/`pipeline_kayit.py`/`tam_tur.py`'de büyük genişleme; ilgili testler `tests/test_pipeline_kayit*.py`, `tests/test_tam_tur*.py`, `tests/test_oa_metrik*.py`, `tests/test_hook*.py`, `tests/test_durum_md.py` ve komşuları.
- **2026-07-28 (v0.5.5 — P0-9 OCR-NÖBETÇİSİ, oa-ingest'in ana paketi, DURUM.md görünürlük ayağı):** `pipeline_kayit.py`'ye yeni `_ocr_bos_uyarisi(kok)` — `_oa/metin/00-kunye.json`'daki `ocr_durum` damgalı ("OCR-BOŞ → GÖRSEL İNCELEME GEREK") kayıtları salt-okur biçimde okuyup listeler (ikinci bir denetim mantığı İCAT ETMEZ, yalnız oa_ingest'in kendi künyesini yansıtır); `_durum_uret`'e "🔴 OCR-BOŞ Uyarısı" bölümü olarak bağlandı — bayat-md uyarısıyla aynı desen (advisory, asla çökmez, dosya yok/okunamazsa sessizce boş liste). Testler: `tests/test_durum_md.py`'ye 2 yeni test (bölüm var/yok).
- **2026-07-29 (saha testi aktivasyon düzeltmeleri + UDF html2udf hattı — Görev A/B1 KÖK SEBEP, Görev C(1)/C(2)):** Saha bulgusu B1 (defter modelin JSONL'e doğrudan yazmasıyla ELLE oluşabiliyor, `pipeline_kayit.py` hiç çağrılmadan) ve B4 (artefaktsız adım "UYGULANDI" beyanı + yanlış hukuk-dalı parçasının işaretlenmesi) kapatıldı — kullanıcının kabul kuralı gereği BLOKLAMA eklenmedi, yalnız AYIRT EDİLEBİLİRLİK: **Görev A (ARAÇ İMZASI):** `--baslat`/`--isle`/`--katman` ile yazılan HER olaya deterministik `sha256(zaman|tip|adım|parça|durum|kanıt+OA_SURUM)[:16]` imzası eklenir (`_imza_hesapla`); imza yok/uyuşmuyorsa olay "model-beyanlı" (elden/JSONL'e doğrudan düşürülmüş), uyuyorsa "araç-imzalı" sayılır — defter append-only kaldığı için elle yazılan satır asla silinmez/reddedilmez, yalnız `_defter_imza_sayaci` ile DURUM.md'nin "Defter Bütünlüğü" bölümünde görünür sayılır. **Görev C(1) (DAL UYUŞMAZLIĞI, advisory):** dosya hukuk davası iken `oa-mudafii`/`oa-musteki-vekili` (veya tersi: ceza dosyasında `oa-sozlesme`) UYGULANDI/GEREKSİZ dışı bir durumla işaretlenirse "DAL UYUŞMAZLIĞI: ... yanlış dosyada mı çalışılıyor" uyarısı üretilir (`_dal_uyusmazlik_uyarilari`); dal, `--baslat --ceza` alanı (kesin) yoksa `_oa/dosya.md`'deki "Dokunduğu hukuk dalları" satırından, o da boşsa dava kökü klasör adı/mahkeme adı kalıplarından (üçüncül ipucu) tahmin edilir — belirsizse UYARI HİÇ ÜRETİLMEZ. **Görev C(2) (KENDİNE-ATIF, advisory metin zenginleştirme):** P0-6 önkoşul kapısı (adım 5/9) artefakt bulamadığında kanıt metni "taslağın/dilekçenin X bölümünde" gibi kendine-atıf bir kalıp taşıyorsa mesaja `[KENDİNE-ATIF: ...]` notu eklenir (bloklama kümesi değişmedi, yalnız mesaj netleşti). Yeni testler: `tests/test_pipeline_kayit_imza.py` (13), `tests/test_pipeline_kayit_gorev_c.py` (dal uyuşmazlığı + kendine-atıf). B5 (UDF elle yazılıp UYAP'ta AÇILMIYOR) ayrıca kapatıldı — bkz. `oa-dilekce/references/degisiklik-gunlugu.md` (P0-10 UDF-REHBER UYUMU + v0.5.5.2 UDF geçerlilik kapısı düzeltmeleri): hat artık yalnız `md → UDF-HTML → udf-cli html2udf`, elle zip/XML üretimi tamamen kaldırıldı.
- **2026-07-29 (P0-B — GÖREV B, ÜRETİM-ANI TETİĞİN İKİNCİ AYAĞI):** Saha bulgusu B3: `--hook-denetle` yalnız Stop/SessionEnd'de (oturum kapanışında) koşuyordu — bir taslak/UDF üretimi ile oturum kapanışı arasında model hiçbir şey çağırmadan uzun süre çalışabiliyor, teslim kapısının görünürlüğü bu boşlukta GECİKİYORDU. Ortak gövde `hook_denetle`'den `_hook_govde_calistir(kok_aday, hook_adi)`ye ÇIKARILDI (davranış bit-düzeyinde korunur); yeni `hook_postwrite(kok=None)` + `--hook-postwrite` CLI bayrağı eklendi, `hooks/hooks.json`ya `PostToolUse` (matcher `Write|Edit`) girdisi bağlandı. HER Write/Edit çağrısında ağır (Gate-G/makbuz/oa_metrik dahil) tam denetimi koşturmak PAHALI olacağından, önce UCUZ bir ön-denetim (`_hook_postwrite_tetikle_mi` — yalnız `os.scandir` + `mtime` + kısa ön-okuma, disk-ağır iş YOK) yalnız "`_oa/cikti` altında SON 60 saniyede dilekçe-şekilli (`_DILEKCE_DESEN` — TEK KAYNAK) yeni bir dosya düştü mü" sorusuna bakar; HAYIR ise ağır gövde HİÇ çağrılmaz. `_oa/defter` yoksa sessizce atlanır (hook_denetle ile simetrik); ASLA bloklamaz (her zaman exit 0), hataya dayanıklıdır. Yeni testler: `tests/test_pipeline_hook_postwrite.py` (ön-denetim birim testleri + CLI uçtan uca + `hook_denetle` refactor regresyon kilidi), `tests/test_hooks_wiring.py` genişlemesi (PostToolUse/matcher/bayrak doğrulaması).
- **2026-07-29 (v0.5.5.1 düzeltme turu 2 — hakem bulguları YENİ-1/YENİ-2, BLOKLAMA EKLENMEDEN):** İkinci bir sahada koşan hakem incelemesi iki artık-kapanmamış bulgu buldu; ikisi de kapı FORMU değil İŞİ denetlensin ilkesiyle giderildi (yeni bloklama YOK). **YENİ-1 (P0-5 makbuz kapısı gevşemesi):** `_surum_v055_var_mi` yalnız `surum_gorulen`e (bu CLI'den geçen olaylara) bakıyordu — defter TAMAMEN elle yazılmışsa (B1 kök sebebi) bu alan hiç dolmuyor ve "v0.5.5 çağında ama damgasız" durumu "gerçekten eski defter" ile ayırt edilemeyip kapı SESSİZCE gevşiyordu. Düzeltme: `surum_gorulen` boşken iki bağımsız İKİNCİL KANIT denenir — defterin `gunluk`ündeki en erken olay zamanı `_MAKBUZ_CAG_TARIHI` (2026-07-28, P0-5'in yürürlüğe girdiği gün) sonrasıysa, YA DA kökte yalnız v0.5.5-ailesinin üretebileceği bir artefakt (`_oa/defter/metrik.json`, `_oa/DURUM.md`) varsa defter ÇAĞ-İÇİ sayılır ve kapı yine BLOKLEYICI olur; kanıt yoksa davranış DEĞİŞMEDİ (yalnız uyarı) ama uyarı metni artık "defterde hiç sürüm damgası yok — elle yazılmış olabilir" diyerek sebebi açıkça söylüyor (eski "eski/önceki-sürüm" alt dizesi geriye uyum için korunur). Testler: `tests/test_teslim_makbuz.py` (4 yeni: zaman-kanıtlı blokleyici, zaman-kanıtsız-eski davranış korunur, artefakt-kanıtlı blokleyici, uyarı-metni-sebep-açıklaması). **YENİ-2 (hook token maliyeti):** `_hook_govde_calistir` her PostToolUse/Stop-SessionEnd tetiğinde `oa_metrik`in tam `[1]-[7]` telemetri tablosunu (ölçülen sahada tek kökte ~5,2 KB) hook STDOUT'una basıyordu — bu, PostToolUse çıktısı modele doğrudan geri beslendiği için ölçülmemiş bir bağlam maliyetiydi. Düzeltme: `metrik.json` yan etkisi HER koşuda yazılmaya devam eder (veri kaybı YOK) ama tam tablo artık hook STDOUT'una BASILMAZ — yalnız `_oa/defter/metrik.json`a yönlendiren tek satırlık bir işaret basılır; tam tablo yeni bir bayrakla (`pipeline_kayit.py --goster --telemetri`) avukatın kendi isteğiyle görünür (varsayılan `--goster` UCUZ kalır, P1-9(b)'deki "sık/ucuz komut" ilkesiyle tutarlı). Denetim/uyarı satırları (kısılan değil) hiç değişmedi. Mevcut throttle (`_hook_cikti_degisti_mi`, aynı-içerikli ardışık hook koşularını sessiz kısa devre yapan mekanizma) doğru çalıştığı SKILL.md'ye tek paragrafla belgelendi (yanlışlıkla "gürültülü" diye sökülmesin diye). Testler: `tests/test_hook_denetle.py` (3 yeni: hook artık tam tabloyu basmaz + metrik.json yan etkisi korunur, varsayılan --goster ucuz kalır, --goster --telemetri tam tabloyu gösterir).
- **2026-07 (v0.5.5.1 — saha testi tetik düzeltmeleri):** Gerçek dosyada (214 evrak, bakir klasör, metodoloji talimatı verilmeden) koşan v0.5.5 saha testinin dersi: **mekanizmalar sağlamdı, çağrılmıyorlardı**. Şüphelenilen dört kusur (Gate A sayfa haritası, `teyit --damga` muhakeme dosyası yazımı, M1 dava tezi bölümü, kayıpsız `--senkron`) sentetik yeniden üretimle sınandı ve DÖRDÜ DE SAĞLAM çıktı — bu yüzden kod değil TETİK düzeltildi. (1) **Working memory tetiği** (`_analiz_md_kendini_onar`): `dosya-analiz.md` bizim biçimimizde değilse (`_iskelet_saglam_mi` False — elle yazılmış/bozulmuş), hook gövdesi `tam_tur.cmd_senkron`'u in-process koşturur ve çalışma evraklarını kayıpsız geri gömer; onarım GÖRÜNÜRDÜR (sessiz onarım da sessiz atlama kadar yasaktır) ve TAMAM ÜRETMEZ — Gate G+ fail-closed kalır. (2) **Defter-muhakeme denge uyarısı** (`_defter_muhakeme_dengesi_uyarisi`): kütükteki DAMGA'lı satır sayısı muhakeme kaydındaki bölüm sayısından fazlaysa uyarır — `teyit --damga` ikisini birlikte yazdığından fark, satırın script DIŞINDA eklendiğini ve o künyelerin muhakemesinin hiç yapılmadığını gösterir (advisory; teslim kapısı `ictihat_muhakeme_denetim.py`'de kalır). (3) **Kök dosya bekçisi** (`_sozlesme_disi_kok_dosyalari`): P1-9(b) yalnız `_oa/` altındaki DİZİNlere bakıyordu, kökteki serbest DOSYALAR kör noktaydı; artık görünür uyarı üretir — bloklamaz (amaç çizgisi: ateşlemeyen kapı eklenmez). Testler: `tests/test_v0551_saha_tetikleri.py` (10). (4) **Makbuz çağ eşiği sabitlendi:** P0-5 geçiş supabı (`_surum_v055_var_mi`) eşiği `_OA_SURUM_TUPLE`ye bağlıydı — 0.5.5 → 0.5.5.1 yaması çıktığı anda sahadaki TÜM v0.5.5 defterleri "eski defter" sayılıp makbuz kapısı SESSİZCE gevşiyordu. Supabın sorusu "bu defter tam güncel sürümde mi" değil "bu defter P0-5 çağına geçmiş mi"dir; eşik `_MAKBUZ_CAG_ESIGI = (0, 5, 5)` olarak çağın açıldığı sürüme sabitlendi ve artık sürüm artışıyla kaymaz (regresyon: `test_yama_surumu_gecis_supabini_GEVSETMEZ`).
- **2026-08-07 (v0.5.7 — BAYAT-TOHUM AŞISI + anayasa m.0, Denizli 754 sahası):** Saha bulgusu: model `_oa/araclar/` kopyalarını yüklü eklentiden değil KOMŞU dava klasöründen almıştı — 20/20 script eski nesildi (ingest v1.1 vs v1.7); Gate A/C, OCR nöbetçisi, makbuz/sha kapıları, DAMGA ve KAYNAK-URL o koşuda fiilen yoktu. AŞI: `_bayat_arac_uyarisi(kok)` kopyaları eklentinin kendi scriptleriyle BAYT-karşılaştırır; bayat kopya `--hook-denetle` çıktısında ve `UserPromptSubmit` enjeksiyonunda (hat açıkken bile) görünür — uyarır, BLOKLAMAZ; tek susturucusu tazelemektir. SKILL 'ARAÇ ÇANTASI' kuralına TAZE-KAYNAK ŞARTI eklendi (her hat başında eklentiden yeniden; komşudan kopya YASAK). Hook katmanı için deterministik yerel doğrulama: `tests/test_hook_komutlari.py` (şema + 4 komutun fiilî sözleşmesi + aşı testleri, 10 test) ve `tools/hook_doktor.py` (kurulu eklentiyi de test eder; bayat-süreç tuzağına karşı canlı doğrulama tarifi basar — saha dersi: katman sağlamken masaüstü uygulamasının güncelleme-öncesi süreci hook kaydını miras bırakmıyordu; çare TAM kapat-aç / /reload-plugins). Anayasa m.0 (kurucu ilke: metodoloji tanım değil DONANIMDIR; yetenekler kullanıcı-model KÖPRÜSÜDÜR) SKILL 'GERÇEK SCRIPT' maddesine bağlandı.
- **2026-08-12 — v0.5.8 fork-prova:** KAYNAK-BLOĞU konvansiyonu (P6+P7): ürün başına kaynaklar@sha8 + besledigi + uretim; tazelik_denetim kapanışta koşturulur.
- **2026-08-13 — v0.5.8.1 tetik paketi (447 dersleri):** hook devir enjeksiyonuna TESLİM DİSİPLİNİ beşlisi + Stop hook'una mühürsüz-teslim nöbetçisi (_muhursuz_teslim_uyarisi).
- **2026-08-13 — v0.5.8.2 HOOK YAPISAL ONARIM:** üç sahada sıfır-ateşleme kök nedeni bulundu (kabuksuz yürütmede '||' zinciri python'a argüman gidiyordu); run-hook.cmd polyglot sarmalayıcı + tek-komutlu hooks.json (superpowers deseni); 4 sarmalayıcı testi.
