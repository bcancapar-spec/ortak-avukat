# oa-illiyet — Değişiklik Günlüğü

(SKILL.md gövdesinden bağlam ekonomisi için taşındı; içerik aynen korunmuştur.)

- **2026-06:** Anayasal düstur işlendi (Can yönlendirmesi): **usul esasa üstündür** — süre usul hukukunun parçasıdır; düstur bu parçanın işlevine operatif kuralla bağlandı (yukarıdaki bölüm). Müvekkil menfaati çift yönlü: kendi usul zaafını sıfırla, karşı tarafın usul hatasını (özellikle kaçırılmış süreyi) tespit et ve derhâl kullan.
- **2026-06:** `oa-usul` eşgüdümü işlendi (Can yönlendirmesi): iki parça bağımsız değil — usul olayları graf düğümü, oa-usul tespitleri kesme etkisiyle grafa geri beslenir; yük taşıyan bağ analizi ortak çıktı.
- **2026-06:** Örnekleme ilkesi bağlandı (anayasal — Can yönlendirmesi): konu sayımları örneklemdir, kapsam tüm Türk hukukudur; işlemeyen örneklem güncellenir, metod sabittir.
- **2026-06:** Çaba/kalite standardı bağlandı (anayasal — Can yönlendirmesi): tasarruf hedef değil; derinlik karmaşıklığa göre yükselir; Opus+High taban.
- **2026-06:** Doğaçlama meşruiyeti bağlandı (anayasal — Can yönlendirmesi): yöntemde serbest doğaçlama (Çapar lafzı), olguda sıfır halüsinasyon/teyit.
- **2026-06:** Başbakan denetimine tabi olma bağlandı (anayasal — Can yönlendirmesi): istisnasız tam işletim, tembellik/kaçış yasağı, dürüst 'yapılamadı' + yeni yöntem.
- **2026-06:** Başbakan halüsinasyon teftişine tabi olma bağlandı (Can yönlendirmesi): künye/madde/düğüm ayrıca MCP-teyit denetiminden geçer; teyitsiz unsur doğrudan dışlanır.

- **2026-07 (v3.16):** Fiziksel aktivasyon — simülasyon yasağı bloğu eklendi (Can yönlendirmesi — komutla tetiklenen parçalar description'dan taklit edilmesin, fiilen çağrılsın): çalıştı = fiilî Skill çağrısı / gerçek script / gerçek MCP çağrısı + DEVİR PAKETİ + pipeline defteri kaydı. Değişiklik günlüğü bağlam ekonomisi için `references/degisiklik-gunlugu.md`'ye taşındı (içerik korunmuştur).
- **2026-07 (v3.17):** Yerel hafıza kuralı bağlandı (Can yönlendirmesi — hafıza ve devir çalışılan klasörde fiziksel yaşar): parçanın kalıcı çıktıları `_oa/` kökünde (defter/devir/teyit/cikti); fiziksel aktivasyon bloğuna işlendi.
- **2026-07 (v3.18):** Çalışma evrakı kuralı: graf `_oa/cikti/01-illiyet-graf.json` standart yoluna bağlandı.
- **2026-07:** Çaba/token düsturu GÜNCELLENDİ (Can yönlendirmesi): tasarruf artık HEDEF — ama yalnız mekanik/temsil katmanında ve VERİ-KAYIPSIZ; muhakemede tasarruf edilmez, derinlik/doğrulama/araştırma asla kısılmaz. Aile geneli anayasal güncelleme; deterministik motor: `oa-ingest`.
- **2026-07 (v3.22 — M2-3):** Sürüm işaretçisi ailenin ortak M2-3 entegrasyon sürümüne hizalandı (`aile_dogrula.py` sürüm tutarlılık uyarısını temizlemek için); bu satırın kendisi dışında bu parçada işlevsel bir değişiklik YOKTUR — gerçek içerik değişiklikleri (varsa) yukarıdaki ayrı kayıtlardadır.
- **2026-07 (v3.26 — M3-4 hizalama):** Sürüm işaretçisi ailenin M3 faz-sonu ortak hizalama sürümüne (v3.26) taşındı (`aile_dogrula.py` sürüm tutarlılık uyarısını kapatmak için); bu satırın kendisi dışında bu parçada işlevsel bir değişiklik YOKTUR — gerçek içerik değişiklikleri (varsa) yukarıdaki ayrı kayıtlardadır.
- **2026-07-28 (v0.5.5 — M5, Paket D):** Yeni "ZAMAN KATMANI — kronoloji + süre pencere bindirme" bölümü — illiyet grafına zaman katmanı bindirilir; `oa-sure/scripts/hesapla_sure.py --pencereler` çıktısı (aynı dönemde çakışan birden fazla süre) bu katmanın girdisidir (`references/cikti-blogu.md` şeması güncellendi).
- **2026-08-12 — v0.5.8 fork-prova:** grafik_denetim --zincir (P3): illiyet zinciri güven analizi — confidence_decay çarpımı + en zayıf halka (oa-antitez beslemesi; bayraksız çıktı DEĞİŞMEZ).

## v0.5.8.4 — 2026-08-15

- **Zincir analizi VARSAYILAN oldu (bilinçli sözleşme değişikliği):** `grafik_denetim.py` illiyet zinciri güven analizini (confidence_decay + en zayıf halka; bölüm 8 + json `zincirler` alanı) artık bayraksız her koşuda üretir — 372 sahasında `--zincir` 0 kez verilmişti, analiz hiç üretilmemişti (opsiyonel kapı = ateşlemeyen kapı; tetik>sertlik dersi). Kapatmak için `--zincirsiz`; eski `--zincir` geriye-uyum için kabul edilen NO-OP. SKILL örnek komutları güncellendi.

## v0.5.16 — 2026-09-07 · İki Denetimin İnfazı (birleşim + entegrasyon)

> Kaynak: iki dış denetim raporu (2026-09-06, `_gorus/denetim-2026-09-06-*.md`), 15 sahiplik grubu (`v0516/<grup>` dalları), Yargı Pro hakem teyitleri (`_gorus/denetim-v0516-yargipro-teyit-<grup>.md`). Aşağıdaki blok(lar) ilgili grubun ajan raporundaki `changelog_notu` metninin aynıdır; entegratör notu birleşim sonrası hizalamayı (H1–H5) kaydeder.

### Grup A — kaynak rapor `03-A-1-uygula.json`

## v0.5.16 — SERT KAPI + dedektör onarımları (bütünleşik denetim 2026-09-06: K2, G6, G1, G5, G7, G8, G3, G4, G2; hamleler 2/5/6; avukat kararları #1 #5 #6)\n\n**scripts/grafik_denetim.py**\n- **K2+G6 (karar #1, SERT kapı):** `rapor()` gövdesi `_rapor_govde()`'ye taşındı ve try/except ile sarıldı → çökmede stdout `DENETİM ÇÖKTÜ: <sınıf>: <mesaj>`, `--json`'a `{arac, denetim_coktu:true, hata, girdi, cikis_kodu:2}`, exit 2. Çevrim VEYA şema hatası → exit 3 (`blok_sinifi: [\"sema\"|\"cevrim\"]`); temiz → exit 0; kullanım hatası exit 1 aynen. `id` eksik düğüm traceback değil şema hatası. Tüm DFS'ler özyinelemesiz (`_kesme_noktalari` Tarjan AP, `yuk_tasiyan_kenarlar` bridge, `_sccs`, `_johnson_cevrimler`, `zincir_analizi`) + `setrecursionlimit` koruması — 5.000 düğümlük zincir/halka çökmez. Eski \"bulguda da exit 0\" karakterizasyonu bilinçli terk edildi (opsiyonel kapı = ateşlemeyen kapı).\n- **G1:** `cevrimleri_bul()` — ≤200 düğümde Johnson tam basit çevrim sayımı, üstünde geri-kenar örneklemi (rapor söyler); çevrimler minimal (A→B→C→B → `[B,C,B]`) ve `_kanonik_cevrim` ile mükerrersiz. `cevrim_var_mi()` geriye uyumlu.\n- **G5:** `yukle()` mükerrer düğüm id → şema hatası `mükerrer id 'x' (N kez)`; ilk kayıt korunur; sözlük/liste olmayan kayıtlar fail-closed.\n- **G7 (karar #6):** `baglanmamis_deliller()` ayrı sınıf — `dayanak_delil` id veya ad-benzerliği (`_delil_eslesir`, difflib ≥0.6, `_tr_kucuk`) ile bağlar; `tip: delil` yetim sınıfından çıktı; §2b + JSON `baglanmamis_deliller`. (HMK m.190 ispat yükü — Mevzuat MCP teyit)\n- **G8:** `kopru_dugumler()` — AP ama yalnız ≥2 bileşen ≥2 düğümlüyse; etiket `perde` (gercek_kisi + ≥2 kanatta tuzel_kisi; TBK m.19 muvazaa — Mevzuat MCP teyit) | `yapisal`; `karar|mahkeme` muaf (`KOPRU_MUAF_TIPLER`). JSON `kopru_dugumler[].etiket`.\n- **G3:** illiyet kenarında `dogrulama` zorunlu (`dogrula_sema` → exit 3); `desteksiz_kenarlar` = dogrulama yok VEYA iddia+delilsiz; `norm_uyarilari()` advisory `[ŞEMA UYARISI]`.\n- **G4 (karar #5):** `GUC_VARSAYILAN` 0.8→0.4; `guc_beyansiz_kenarlar()` ayrı sınıf + §3 satırı + JSON alanı.\n- **G2:** köksüz illiyet grafı → §8 `çevrim nedeniyle kök yok — GÜVENİLMEZ` (`zincir_uyarisi`); çevrimde §7 `yük taşıyan kenar çevrimde anlamsız`.\n- JSON sözleşmesi (B bekçisi): `denetim_coktu`, `cikis_kodu`, `blok_sinifi`, `baglanmamis_deliller`, `guc_beyansiz_kenarlar`, `kopru_dugumler[].etiket`, `zincir_uyarisi` eklendi; eski alanlar korundu.\n\n**SKILL.md:** örnek komutta `--json` ZORUNLU (köşeli parantez kaldırıldı), exit kodu tablosu, yeni sınıflar. **references/illiyet-doktrini.md:** \"Şema sertliği (v0.5.16)\" bloğu. **references/cikti-blogu.md:** exit kodu, §2b bağlanmamış delil, köprü etiketi, güç beyansız satırları.\n\n**Testler:** `tests/test_v0516_A.py` (yeni, 37); `test_grafik_denetim.py`, `test_zincir_analizi.py` karakterizasyon kilitleri bilinçli güncellendi (docstring'de bulgu kodu + gerekçe).

### Grup A — kaynak rapor `25-A-2-uygula.json`

## v0.5.16 — ŞEMA GENİŞLETME + DAL + TARAF (bütünleşik denetim 2026-09-06: G10, G9, G12; hamleler 7/8; avukat kararları #7 #8 #9 #11)

**scripts/grafik_denetim.py**
- **G10 (karar #7, şema KIRILDI + göç):** `KANONIK["kesme_flag"]` dal önekli (`KESME_DALLARI`: medeni: mucbir_sebep/magdur_kusuru/ucuncu_kisi_kusuru · miras: paylastirma_kasti/ivaz · ceza: izin_verilen_risk/kendi_tehlikesine_girme/hukuka_uygunluk/magdur_kusuru). Eski çıplak değer `[ŞEMA UYARISI]` + "--goc ile dal etiketle" + dal önekli adaylar (exit değişmez — geriye uyum). §6 sabit doktrin hatırlatması (`KESME_NOTLARI`, HÜKÜM değil): `ceza:magdur_kusuru` "illiyeti KESMEZ — kusur derecesine/ceza miktarına etki eder (Yargıtay 12. CD yerleşik hattı; künye KÜTÜKTEN; TCK m.22/4-5 — Mevzuat MCP teyit 2026-09-07)", `miras:paylastirma_kasti` "muris muvazaasında bozma gerekçesi — ispat ölçütü". Yeni alt komut `--goc <eski.json> --dal medeni|miras|ceza --cikti <yeni.json>` (`goc()`, `goc_komutu()`): kaynağa dokunmaz, değişen kenar sayısını basar, hedef dalda kanonik olmayan değer göçmez ("elle düzelt"); GÖÇ ÇÖKTÜ → exit 2. JSON `kesme_adaylari[].{kesme_flag, dal, not}` (`_kesme_ref`).
- **G9 (karar #8, defterden + CLI override):** `--taraf sanik|mudafii|katilan|musteki|davaci|davali|alacakli|borclu` veya `--kok <dava kökü>` (`defterden_taraf()`: `_oa/defter/pipeline-durum.json` `ceza_dali` — mudafii→sanık, musteki→müşteki; yok/bozuk/boş → fail-closed "taraf bilinmiyor — --taraf ver"). `taraf_yonu()`: savunma kanadı → `curut`, iddia kanadı → `kur`. `curut` yönünde §6 "kesme savunmasını KUR", §7 "karşı tarafın bu bağını ÇÜRÜT", §8 "karşı tarafın EN ZAYIF halkası". JSON `taraf`, `yon`. `--taraf` defteri ezer.
- **G12 (karar #9, tip):** `KANONIK["tip"]` += `karar`, `mahkeme`; `KANONIK["sonuc"]` {onadi, bozdu, kaldirdi, geri_cevirdi, esastan_ret, kesin} (HMK m.353/370, CMK m.280/302 — Mevzuat MCP teyit 2026-09-07); `KENAR_ALANLARI` += `sonuc`. `tur: kanun_yolu` → `sonuc` zorunlu (exit 3); karar/mahkeme dışı uç ve kategori `illiyet` → `[ŞEMA UYARISI]` (`kanun_yolu_uyarilari`). `_illiyet_kenar_mi()` ile kanun_yolu kenarları çevrim/yük/zincir/kök/güç-beyansız/kesme hesaplarından dışlanır. `kanun_yolu_zinciri()` + **§9 KANUN YOLU ZİNCİRİ** + JSON `kanun_yolu_zinciri[].{yol, sonuclar}`. Karar/mahkeme köprü muafiyeti (A-1) doğrulandı.

**SKILL.md:** karar/mahkeme tipi, kanun_yolu türü, kesme_flag dal listesi + "doktrin hatırlatması — hüküm değil" sınırı, `--taraf/--kok`, `--goc`, exit tablosu ve JSON alanları. **references/illiyet-doktrini.md:** §1 medeni/miras kesme (TBK m.52), §2 ceza kesme + ceza:magdur_kusuru hatırlatması (TCK m.22), §4 ticaret modelleme şablonu (karar #11: organik bağ ve perde için AYRI `hak` düğümleri — "alternatif değil, birlikte"; künye kütükten), §5 taraf yönü, §6 şema + **ENUM TABLOSU (doktrin↔kod kilidi)** + kanun yolu + göç. **references/cikti-blogu.md:** dal önekli kesme satırı, ticaret modelleme bloğu, kanun yolu zinciri bloğu, yön satırları.

**Testler:** `tests/test_v0516_A.py` +37 (toplam 74); `test_grafik_denetim.py` kesme ref anahtar seti (`KESME_REF_ANAHTARLARI`) ve JSON anahtar seti kilidi BİLİNÇLİ güncellendi (docstring: G10/G9/G12); `test_v059_saf_semasi.py` JSON anahtar seti (sahiplik dışı, en küçük düzeltme).
