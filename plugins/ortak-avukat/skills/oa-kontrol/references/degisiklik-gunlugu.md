# oa-kontrol — Değişiklik Günlüğü

(SKILL.md gövdesinden bağlam ekonomisi için taşındı; içerik aynen korunmuştur.)

- **2026-06 (v1):** İlk sürüm — atıf denetimi + teslim öncesi + zaaf protokolü; gerçek dosya tecrübesinden anonimleştirilmiş örüntüler hâlinde damıtıldı (zaaf, içtihat değişikliği, istihkak). Deterministik `oa-antitez` scripti için protokol temeli olarak işaretlendi.
- **2026-06 (v1.1):** Aktif fırsat taraması (D) eklendi (Can yönlendirmesi): zaaf avının yanında kullanılmamış lehe açıları ara; her eksik için düzeltme + iyileştirme öner.
- **2026-06 (v1.2):** İfşa kontrolü eklendi (Can yönlendirmesi): teslim öncesi, sunulan metnin karşı tarafın henüz ileri sürmediği bir antitezi/zaafı ele verip vermediği denetlenir; veriyorsa çıkarılır, cephanelikte dahili kalır.
- **2026-06:** Anayasal düstur işlendi (Can yönlendirmesi): **usul esasa üstündür** — süre usul hukukunun parçasıdır; düstur bu parçanın işlevine operatif kuralla bağlandı (yukarıdaki bölüm). Müvekkil menfaati çift yönlü: kendi usul zaafını sıfırla, karşı tarafın usul hatasını (özellikle kaçırılmış süreyi) tespit et ve derhâl kullan.
- **2026-06:** Örnekleme ilkesi bağlandı (anayasal — Can yönlendirmesi): konu sayımları örneklemdir, kapsam tüm Türk hukukudur; işlemeyen örneklem güncellenir, metod sabittir.
- **2026-06:** Çaba/kalite standardı bağlandı (anayasal — Can yönlendirmesi): tasarruf hedef değil; derinlik karmaşıklığa göre yükselir; Opus+High taban.
- **2026-06:** Doğaçlama meşruiyeti bağlandı (anayasal — Can yönlendirmesi): yöntemde serbest doğaçlama (Çapar lafzı), olguda sıfır halüsinasyon/teyit.
- **2026-06:** Başbakan denetimine tabi olma bağlandı (anayasal — Can yönlendirmesi): istisnasız tam işletim, tembellik/kaçış yasağı, dürüst 'yapılamadı' + yeni yöntem.
- **2026-06:** Müvekkil-aleyhi dış çıktı yasağı bağlandı (anayasal — Can): iç analizde dürüst zaaf, dış belgede müvekkil lehine kurgu.
- **2026-06:** Manifest kapatma denetimi eklendi (Can yönlendirmesi): teslim öncesi her evrakın işlendiği sayımla doğrulanır; atlanan/okunamayan açıkça bildirilir.

- **2026-07 (v3.16):** Fiziksel aktivasyon — simülasyon yasağı bloğu eklendi (Can yönlendirmesi — komutla tetiklenen parçalar description'dan taklit edilmesin, fiilen çağrılsın): çalıştı = fiilî Skill çağrısı / gerçek script / gerçek MCP çağrısı + DEVİR PAKETİ + pipeline defteri kaydı. Değişiklik günlüğü bağlam ekonomisi için `references/degisiklik-gunlugu.md`'ye taşındı (içerik korunmuştur).
- **2026-07 (v3.17):** Yerel hafıza kuralı bağlandı (Can yönlendirmesi — hafıza ve devir çalışılan klasörde fiziksel yaşar): parçanın kalıcı çıktıları `_oa/` kökünde (defter/devir/teyit/cikti); fiziksel aktivasyon bloğuna işlendi.
- **2026-07:** Çaba/token düsturu GÜNCELLENDİ (Can yönlendirmesi): tasarruf artık HEDEF — ama yalnız mekanik/temsil katmanında ve VERİ-KAYIPSIZ; muhakemede tasarruf edilmez, derinlik/doğrulama/araştırma asla kısılmaz. Aile geneli anayasal güncelleme; deterministik motor: `oa-ingest`.
- **2026-07 (v3.21 — M2-1):** İçtihat Muhakeme Zinciri şema+model paketi (Fable-planlı): A listesine "içtihat muhakeme edildi mi (DAMGA)" + "Yargıtay/BAM atfı var mı" kalemleri eklendi. Yeni bölüm "MUHAKEME adımı (İçtihat Muhakeme Zinciri)": bu parçanın `oa-kiyas` ile birlikte MUHAKEME adımını (KUNYE/KAYNAK-IZI/İLGİLİ-KISIM/İLLİYET/DAMGA/AYIRT-ETME) denetlediği açıkça yazıldı; ALEYHE'nin dış çıktıya girmeyip iç analizde zorunlu işlenmesi + fail-closed NOTR doktrini bağlandı (şema: `oa-kiyas/references/ictihat-muhakeme-sablonu.md`). Denetim scripti sonraki pakette.
- **2026-07 (v3.21 — M2-2):** İçtihat Muhakeme Zinciri MEKANİK KAPISI teslim edildi: `scripts/ictihat_muhakeme_denetim.py` (+ paylaşımlı `scripts/kunye_ortak.py` yardımcısı) — G1 (emsal içtihat taraması, uyarı) / G2 (çıplak atıf + kaynak-izi + alan bütünlüğü, engel) / G3 (DAMGA'ya göre ALEYHE/ALEYHE-AYIRT/damgasız-geçersiz engeli, NOTR uyarısı) üç kapısı; MEKANİK KAPILAR bülteni + A listesi kalemi SKILL.md'ye işlendi. Testler: `tests/test_ictihat_muhakeme_denetim.py`.
- **2026-07 (v3.22 — M2-2 düzeltme):** KRİTİK denetçi bulgusu giderildi — `eslesir()`/`_atif_icin_kayit_bul()` yalnız (esas, karar) çiftine bakıyordu, DAİREYİ (merciyi) yok sayıyordu; Türk yargı sisteminde esas/karar no'ları HER dairede yılda sıfırdan başladığından aynı numaralara sahip FARKLI dairelerin (biri LEHE, biri ALEYHE) kayıtları çakıştığında script yanlış (LEHE) kayda eşleşip ALEYHE atfın teslim engelinden sessizce kaçmasına izin verebiliyordu (bağımsız PoC ile fiilen doğrulandı). Düzeltme: `MuhakemeKaydi.daire` alanı (`kunye_ortak.daire_key`) eklendi; `eslesir()`/`_atif_icin_kayit_bul()` artık daire de biliniyorsa daireyi de eşleştiriyor; dilekçe atfı daire belirtmiyorsa VE aynı esas/karar no'suna sahip birden fazla FARKLI daireye ait kayıt varsa yeni fail-closed ENGEL ("hangi kaydın geçerli olduğu belirlenemiyor") eklendi; `taslaktaki_atiflari_bul()`'daki tekilleştirme anahtarına da daire eklendi (aynı esas/karar, farklı daireye ait iki ayrı atıf artık birleştirilip kaybolmuyor). `rapor_yaz()`'daki ölü `kutuk_bos_mu` parametresi işlevsel hale getirildi (boş muhakeme dizini bilgi notu). Yeni testler: çok-daireli çakışma pozitif/negatif/belirsizlik senaryoları (`tests/test_ictihat_muhakeme_denetim.py`).
- **2026-07 (v3.23 — M3-2, G1 "esaslı dilekçe" tip listesi/R6):** `ictihat_muhakeme_denetim.py`'ye `--tip` bayrağı + `ESASLI_OLMAYAN_TIPLER = {"yemin", "idari-kanal"}` + `esasli_mi(tip)` eklendi: G1 "emsal içtihat yok — muhakeme zayıf" UYARISI artık yalnız "esaslı" dilekçe tiplerinde (dava/cevap/istinaf/temyiz/aym_bireysel) basılıyor; `yemin`/`idari-kanal` gibi hafif tiplerde [BİLGİ]'ye düşüyor (G1 zaten hiçbir hâlde bloklamıyordu — bu yalnız gürültüyü tip'e göre süzüyor). `tip` verilmezse veya tanınmayan bir değerse FAIL-SAFE: esaslı sayılır (uyarı bastırılmaz). G2/G3 (çıplak atıf / ALEYHE / eksik alan) engelleri `--tip`'ten HİÇ ETKİLENMEZ — yalnız G1'in metni/etiketi değişir. Bağlantı: `dilekce_denetim.py`'nin [F] kapısı (`ictihat_muhakeme_kapisi()`) artık `--tip` değerini kardeş scripte aktarıyor; `teslim_paketi.py`'nin (b2) çağrısına da `--tip` eklendi (tek komut teslim zincirinde aynı davranış). Yeni testler: `tests/test_ictihat_muhakeme_denetim.py` (`esasli_mi` sözleşmesi, `--tip` CLI davranışı, G2/G3'ün tip'ten bağımsızlığı).
- **2026-07 (v3.24 — M3-3):** (R4) "İLLİYET" alanı şemada **DAVAYA-BAĞ** oldu; `ictihat_muhakeme_denetim.py` yeni başlığı okur, eski "İLLİYET" başlığını geriye dönük olarak da kabul eder (kayıpsızlık) — SKILL.md'deki MEKANİK KAPILAR + MUHAKEME adımı bölümleri güncellendi. (R2) "Dört yeşil ışık" sayma dili kaldırıldı; tek ve yetkili teslim ölçütü artık `teslim_paketi.py`'nin exit kodu olarak yazılı — alt kapıları elle sayma yasağı açıkça eklendi. (YENİ-2) `ictihat_muhakeme_denetim.py`'ye aynı esas/karar/daireye ait birden çok kayıt arasında ÇELİŞEN DAMGA (ör. aynı karara hem LEHE hem ALEYHE) varsa UYARI (bloklamaz) üreten `_celisen_damga_uyarisi()` eklendi — script artık temiz adayı sessizce seçip ALEYHE ikizini gölgelemiyor. Yeni testler: `tests/test_ictihat_muhakeme_denetim.py` (DAVAYA-BAĞ alan adı, eski başlık geriye-dönük okuma, çelişen damga uyarısı pozitif/negatif).
- **2026-07 (v3.26 — M3-4 hizalama):** Sürüm işaretçisi ailenin M3 faz-sonu ortak hizalama sürümüne (v3.26) taşındı (`aile_dogrula.py` sürüm tutarlılık uyarısını kapatmak için); bu satırın kendisi dışında bu parçada işlevsel bir değişiklik YOKTUR — gerçek içerik değişiklikleri (varsa) yukarıdaki ayrı kayıtlardadır.
- **2026-07-28 (v0.5.5 — P0-5/P0-6, Paket A+B):** `teslim_paketi.py` artık `_oa/defter/teslim-makbuz.json` (başarı) / `teslim-makbuz-red.json` (başarısızlık) üretir (`OA_SURUM="0.5.5"` olay-bazlı damga); `pipeline_kayit.py` İN-PROCESS import edilir (subprocess sınıfı + env-kapatma yüzeyi kalktı); "script bulunamadı" artık FAIL-CLOSED (P0-5d). Önkoşul-artefakt kapısı (P0-6, kademeli blokleyici/uyarı) ve `kunye_ortak.py`/`ictihat_muhakeme_denetim.py`'ye çok-daireli/çelişen-damga sağlamlaştırmaları eklendi.
- **2026-07-28 (v0.5.5 düzeltme turu — Ş2, İKİ BLOKER + ÖNEMLİ, HAYALET MUHAKEME kütük-dayanağı ikinci tur):** (1) `kutuk_dayanagi_denetle` artık yalnız "esas/karar kütükte HERHANGİ bir satırda geçiyor mu" (`kutukte_esas_karar_satiri_var_mi`) sormuyor — damgasız/tam-metinsiz ucuz bir ARAMA teyidi (`teyit --arac ictihat_ara`, döküm-icerik'siz, --damga'sız, kod=0) bu denetimi bedavaya geçiyordu (t3-B). Yeni `kunye_ortak.kutukte_damgali_dayanak_satiri_var_mi` bölümün DAMGA'sıyla AYNI `DAMGA=` tokenını taşıyan VE aynı KAYNAK-IZI dosyasını gösteren gerçek bir satır arar; yoksa HAYALET MUHAKEME BLOK'u (farklı, daha isabetli gerekçeyle) üretir. (2) `kutuk_dayanagi_denetle`'nin kütük fiilen kullanılmıyorken (`kutuk_gercek_veri_var_mi` False — taze kök/derin-yol) SESSİZCE atlanması (geriye uyum, bit düzeyinde KORUNUR) artık GÖRÜNMEZ değil — `rapor_yaz` yeni `kutuk_kullanimda_mi` parametresiyle "[BİLGİ] ... HAYALET MUHAKEME denetimi ... ÇALIŞMADI" satırını basar (bloklamaz — yalnız görünürlük, "sessiz atlama yasağı"). Kardeş düzeltme (`oa-pipeline/scripts/oa_hafiza.py`): `_muhakeme_kacis`'in CR (lone `\r`) satır-sonu kaçış atlatması (t9 — `(?m)^` yalnız `\n` sonrasını kaçışlar, ama okuma anında universal-newlines lone `\r`'yi `\n`'e çevirdiğinden aradaki asimetri bir hayalet bölümün kaçış katmanını hiç tetiklemeden sızmasına izin veriyordu) `_satir_sonu_normalize` ön-adımıyla kapatıldı. Yeni testler: `tests/test_oa_hafiza_arac_guvenlik.py` (t3-B izole + sb6 birleşik CR+ARAMA zinciri + `_muhakeme_kacis`/`_satir_sonu_normalize`/`kutukte_damgali_dayanak_satiri_var_mi` birim testleri), `tests/test_ictihat_muhakeme_coklu_bolum.py` (görünürlük bilgi satırı pozitif/negatif; `test_kutuk_son_damga_ayni_ise_ok` gerçek KAYNAK-IZI eşleşmesine güncellendi).
- **2026-07-28 (P1-11 doktrin 4 kural — ek paket):** B. Teslim öncesi kontrol listesine "Üslup (ÖMERALP ÜSLUP BAĞLAMASI)" maddesi eklendi: üslup `oa-dilekce/references/kanun-yolu-mimari-playbook.md`'ye (tez-omurgalı, akıcı, bütünsel bağlantılı) uygun mu; GÖRÜNMEZ İSKELET etiket olarak sızmamış mı ([H]), karşı-taraf-kusuru bağlamında onarma-talebi kurulmamış mı ([I]) — `dilekce_denetim.py`'nin [H]/[I] advisory uyarıları gözden geçirildi mi?
- **2026-07-29 (saha testi aktivasyon düzeltmeleri + UDF html2udf hattı — GÖREV A/B2 kapanışı, GÖREV D/B5 kapanışı):** **GÖREV A (B2 — DURUM.md doğmadı):** `_makbuz_yaz` artık HER makbuz yazımından (başarılı VEYA RED) sonra `pipeline_kayit._durum_md_yaz(kok)`'u TEK KAYNAKTAN çağırıp DURUM.md'yi tazeler — avukat görünürlüğü artık yalnız `pipeline_kayit.py --isle/--katman`'a değil, teslim akışının kendisine de bağlı (defter hiç açılmamışsa best-effort no-op, ASLA çökmez/bloklamaz). **GÖREV D (B5 — KRİTİK, UDF elle yazılıp UYAP'ta AÇILMIYORDU):** otomatik ön-kapının UDF üretim adımından `--yerel-motor` (ağsız, hand-rolled zip/content.xml) TAMAMEN KALDIRILDI — saha dosyası A sahasında bu motorun çıktısı UYAP editöründe açılmıyordu ama zincir yine de "TESLİME HAZIR" basıyordu (sessiz-yanlış). `OA_SURUM` "0.5.5.2"ye taşındı (olay-bazlı makbuz damgası). Artık TEK yazıcı gerçek `udf-cli html2udf` (ağ+oturum ister); npx/oturum yoksa bu adım FAIL-CLOSED BLOK olur — bozuk-ama-"üretildi" görünen bir UDF, dürüst bir "ağ/oturum eksik" engelinden daha kötüdür. Ayrıntı ve UDF geçerlilik-kapısı düzeltmeleri (yanlış-BLOK + resmî okuyucu tanığı): `oa-dilekce/references/degisiklik-gunlugu.md`.
- **2026-07 (v0.5.5.3 — BAĞIMSIZ İÇERİK HAKEMİ zorunlu adım):** 2026/307 saha vakası: künye/şablon/defter kapılarının TÜMÜ yeşilken, ayrı bir denetçi "dosya evrakına karşı çürütmeye çalış" brifiyle koşturulduğunda 1 KRİTİK hata çıktı — nakden tazmin savunmasındaki mükerrerlik kurgusu ARİTMETİK olarak çelişikti (karşı tarafın 836 rakamı zaten 1100−264 idi, yani 264 hesabın DIŞINDAYDI; taslak "836'nın içinde" diyordu) ve dilekçe KENDİ başka bölümüyle çelişiyordu. Yanında 6 küçük bulgu (alıntı sadakati — kip uyarlaması tırnak İÇİNDE yapılamaz; genelleme×kronoloji çapraz kontrolü; dayanaksız olgusal cümle; belge NİTELEME hatası; yer tutucu artığı). SKILL.md'ye `C2. BAĞIMSIZ İÇERİK HAKEMİ` bölümü eklendi (beş maddelik kontrol listesi). Mekanik gözü: `dilekce_denetim.py` `[J] SAYI/TARİH HARİTASI` — advisory, ASLA bloklamaz.
- **2026-08-07 (v0.5.7 — [G4] KAYNAK-URL TUTARLILIĞI, Denizli 754 sahası):** `ictihat_muhakeme_denetim.py`'ye [G4] eklendi: (a) künye satırının ±1 satır penceresinde görünen http(s) bağlantısı HİÇBİR muhakeme kaydının `**KAYNAK-URL:**` alanıyla örtüşmüyorsa TESLİM ENGELİ (uydurma bağlantı 'teyit edildi' der — çıplak künyeden DAHA KÖTÜ); (b) teyit anında kaydedilmiş bağlantı dilekçeye işlenmemişse görünür UYARI (bloklamaz — araç URL döndürmemişse künye bağlantısız yazılır, uydurulmaz). `MuhakemeKaydi`ya `kaynak_url` alanı (KAYNAK_URL_LINE_RE) eklendi. Saha gerçeği: 754 koşusunda bayat `oa_hafiza` kopyası `--kaynak-url`'i bilmediğinden 11 teyitli karar dilekçeye bağlantısız girmişti — zincirin dilekçe ucu artık mekanik. Testler: `tests/test_kaynak_url_tutarlilik.py` (4).
- **2026-08-12 — v0.5.8 fork-prova:** [G5] AŞILMIŞ-İÇTİHAT kapısı (LEHE+aşılmış+atıf=BLOK) + tazelik_denetim.py (P6, graft Sources@hash deseni, advisory) + muhur_yaz.py (P1, PROV-O hizalı oa-muhur/1.0; --dogrula sert, yazım zarif-bozulma). Semantica/graft devşirmesi — m.0 protokolü, Can kararları 2026-08-12.

## v0.5.13 — DAHİLİ SIZINTI KAPISI
- `_dahili_belge_mi` + `_uyap_urunler` süzgeci: dahili filigranı taşıyan iç
  analiz belgeleri (celse kartı, cephanelik türevleri) dış-çıktı ürünü olarak
  KOPYALANAMAZ. İkili ürünler (.udf/.pdf/.docx) etkilenmez; okunamayan dosya
  güvenli tarafa düşer (dışa çıkarılmaz). Gerekçe: pratikçi hakem heyeti tez 4;
  mühendis M1/M2 daraltması (yeni parça açılmadı — CI'ın 20-skill kapısı korundu).

## v0.5.12 — İÇTİHAT KAYNAKÇASI (link zinciri tamamlayıcısı)
- Avukat kuralı (2026-08-27): çalışmaya giren her kararın linki TÜM
  çıktılarda. Yeni `kaynakca_uret.py`: taslaktaki karar künyelerini
  (esas/karar çifti) muhakeme kaydının KAYNAK-URL satırlarıyla eşler ve
  taslağın sonuna işaretli, İDEMPOTENT '## İÇTİHAT KAYNAKÇASI' bloğu işler.
  Link ÜRETİLMEZ (yalnız teyitten gelir); URL'siz künye gizlenmez, görünür
  notla listelenir. `teslim_paketi` bloğu UDF üretiminden ÖNCE işler →
  linkler UDF/PDF/40-UYAP kopyalarına kendiliğinden taşınır; makbuza
  `ictihat_kaynakca` (linkli/linksiz) alanı düşer. Kapı DEĞİL, tamamlayıcı.

## v0.5.11 — makbuzda oturum izi (1865-T4a)
- `_makbuz_taban` her makbuza `oturum_izi` alanı ekler (hook son-iz
  köprüsünden; kesin kimlik değil, en-son-aktif-oturum — alan adı dürüst).
  Çok-oturumlu koşuda "bu makbuz hangi oturumun ürünü" sorusu yanıtlanabilir.

## v0.5.10 — FİLO-TAZELİK KAPISI (307 karnesi K1+K2)
- `teslim_paketi.py` yeni BLOKLAYICI kapı `(+++) FİLO-TAZELİK`: dava kökü +
  40-UYAP'taki TÜM teslim-sınıfı .udf'ler mühür-tazelik hükmünden geçer —
  prov'lu-bayat ve 40-UYAP-mühürsüz → RED; kökte mühürsüz → ADVISORY (UYAP
  kaynak evrakı olabilir; yanlış-BLOK yasağı). Kayıtların TAMAMI makbuza
  `teslim_sinifi_urunler` olarak girer (K2: makbuz seçili ürünle sınırlı kalamaz).
- `_uyap_disa_kopya`: kopya adında çift-uzantı soyulur; `.prov.json` mühürü
  kopyayla BİRLİKTE gider (prov'suz kopya sonraki koşuda haklı RED yerdi).

## v0.5.8.4 — 2026-08-15

- **teslim_paketi yeni kapıları (372 Torbalı devşirmesi):** MEVCUT-UDF DEVRALMA (geçerli aday yeniden üretilmez, `udf_devralindi` makbuza; geçersiz elle-üretim adayı `_oa/arsiv-yerel/gecersiz-elle-udf/` karantinasına — silinmez), PROV-TAZELİK (bayat mühür = RED), YEREL-DAMGA (yerel motor ürünü teslime giremez = RED), ŞEKİL (pageFormat 4×42.52 pt değilse udf_yaz kenar yaması otomatik + mühür sha güncellenir; LineSpacing 0.50 ve 11pt link istişari), OTOMATİK MÜHÜR (mühürsüz UDF teslimde teslim_paketi tarafından mühürlenir), MAKBUZ GARANTİSİ (try/finally — erken çıkışlar dahil her başarısız yol RED makbuzu düşürür; sebep + argv kayıtlı), TAZELİK BİLGİ KAPISI (tazelik_denetim advisory; bulgular makbuzda `tazelik_uyarilari`).
- **muhur_yaz:** `--tasi ESKI YENI` — mühür-dosya çifti birlikte taşınır, `artifact_file` güncellenir, sha değişmişse RET (taşıma içerik değiştirmez); `--llm` boş bırakılırsa 'mekanik üretim; içerik oturum LLM koşusundan' otomatik beyanı yazılır.
- **YENİ ARAÇ kaynak_blogu.py:** `<!-- kaynaklar: yol@sha8 · ... -->` bloğunu üretir (tazelik_denetim regexleriyle birebir round-trip; sha'yı script hesaplar, model değil); ürün başına ilk satıra konur; SKILL'e kullanım talimatı eklendi.
- Makbuz gövdesi tek üreticiye (`_makbuz_taban`) tekilleştirildi; şema alanları her yolda aynı + `udf_devralindi`/`kenar_duzeltildi`/`tazelik_uyarilari`/`sebep`/`argv` alanları eklendi.

## v0.5.8.5 — 2026-08-16

- **B4 — RED makbuzda advisory rapor:** `teslim_paketi.py` ilk-engelde-dur exit davranışını DEĞİŞTİRMEDEN engelleyici-olmayan denetimleri (devralma-aday raporu, şekil, prov-tazelik, yerel-damga, tazelik) RED yolunda da koşar; RED makbuzu `advisory_denetimler` alanı kazanır (saha kanıtı: künye BLOK'u kenar ihlalini görünmez bırakmıştı). Advisory satırlar kapı değildir — exit'i etkilemez, dosya sistemine dokunmaz.
- **B5 — E-İMZA MÜHÜR HALKASI:** `muhur_yaz.py` `sign.sgn` tespitiyle tipi otomatik `e-imzali-nusha` yapar (`entity_type` enumuna eklendi; `e_imza_muhur_uret` yardımcısı — `was_derived_from` = imza-öncesi sha). `teslim_paketi.py`: sign.sgn'li dosyada sha uyuşmazlığı BAYAT değil TÜREV sayılır; zincir kurulmuşsa YEŞİL, kurulmamışsa "imzalı türev mühürsüz" uyarısı + best-effort e-imzali-nusha mührü + istisna defterine `dogrulama-toleransi` satırı. İmzasız dosyada PROV-BAYAT RED'i aynen sürer.
- **E-İMZA GUARD:** imzalı nüshada kenar yaması ASLA uygulanmaz (yama zip'i yeniden yazar, e-imzayı bozar) — yama YOK, RED YOK; sapma görünür uyarı + makbuzda `sekil_imzali_sapma` alanı + istisna defteri kaydı. Düzeltme imza öncesi sürümde yapılıp yeniden imzalanır.
- **A4.8 — Teslim sunum disiplini SKILL'e işlendi:** durum sorusuna makbuz OKUNARAK cevap verilir; RED makbuz kullanıcı mesajında GİZLENEMEZ (özetin ilk satırlarında sebep + kapanan kapı); teslimde ürünün TAM yerel yolu + "mühürleri (.prov.json) birlikte taşıyın" notu (`muhur_yaz.py --tasi`).
- **İSTİSNA DEFTERİ şeması belgelendi:** `_oa/defter/istisna-kayitlari.jsonl` — append-only; satır `{zaman, tur, ilgili, gerekce, onay, imza}`. Yazan araçlar: `kunye_teyit.py` (kunye-istisna — B1 kendi-dosya-no muafiyeti), `dilekce_denetim.py` (yanlis-pozitif-ilani — --istisna-gerekce), `gizlilik_tara.py` (gizlilik-deny-override), `teslim_paketi.py`/`udf_yaz.py`/`pipeline_kayit.py` (dogrulama-toleransi — e-imza toleransları). Hiçbir muafiyet/tolerans sessiz kalmaz.
- **B1 — `kunye_teyit.py` kendi-dosya-no istisnası:** taslağın başlık/künye bloğundaki KENDİ dosya numarası satırı ("DOSYA NO : 2024/123 Esas") içtihat künyesi sanılmaz — muafiyet DAR (etiketle başlayan + daire anmayan + E.+K. çifti taşımayan satır); muafiyet [BİLGİ] satırı + istisna defteri kaydıyla görünür (sessiz atlama yasağı).

## v0.5.8.6 — 2026-08-18
- kunye_teyit: kütük Döküm bağlarında `_oa/teyit/ham/` dizini meşru döküm evreni sayılır
  (dokum/ ile eşdeğer; kök-dışı bağ yine RET) — 777 sahasında dökümler ham/'a inmişti.

## v0.5.9 — 2026-08-22 (ÇIKTI ŞEMASI paketi — 40-UYAP)
- **A2 KURUCU (`teslim_paketi.py`):** YEŞİL makbuz yolunda dava kökünde muhatap-nötr
  dış-çıktı dizini `40-UYAP/` doğar: nihai ürün KOPYALARI (UDF + varsa aynı kök-adlı
  PDF/DOCX — taşıma DEĞİL, tek-nüsha ilkesi: asıl `_oa/cikti`de mührünün yanında kalır)
  + `_damga` alanlı `teslim-makbuz-KOPYA.json` (sembolik link YASAK — Windows güvenilmez,
  kopya zip-taşınabilir). Makbuza `uyap_kopya` + `uyap_urun_kopyalari` alanları eklendi.
  ADVISORY doğuş: kopya hatası teslimi KIRMAZ (görünür uyarı; exit değişmez); makbuz
  RED/yokken 40-UYAP üretilmez.
- **A1 DOKTRİN:** `references/cikti-semasi.md` (YENİ) — 40-UYAP tanımı (dışa giden HER
  şey; muhatap-nötr/yön-belirli), tek-nüsha ilkesi, KOPYA damgası kuralı, advisory doğuş
  + veriyle kapıya terfi yolu (2-3 saha sonrası tetiklenme sayısıyla — karar avukatta),
  A3/A4 bekçilerinin v0.5.10'a ertelendiği not.
- **A5 ATIF:** SKILL.md teslim bölümüne 40-UYAP paragrafı (avukat UYAP'a yüklerken
  40-UYAP'taki kopyayı kullanır; 00-TESLIM notuna 40-UYAP yolu satırı).
- Testler: `tests/test_v059_cikti_semasi.py` (yeşil yol doğuşu + sha-eşit kopya + damgalı
  makbuz-kopyası + RED'de doğmama + kopya-hatası advisory + eski makbuz şeması regresyonsuz).

- **2026-08-31 (v0.5.14 — TESLİM + KÜNYE + KAYNAKÇA paketi):** 2026-08-31 çelişki/kırık denetiminin B-2, B-3, B-13, B-14, B-18, B-24, B-29, B-30, B-33 bulguları infaz edildi. Her kanıt uygulamadan ÖNCE bağımsız olarak yeniden koşturuldu; regresyon seti `tests/test_v0514_teslim.py` (53 test).
  - **B-2 (P0) — künye/atıf kapısı körlüğü, `kunye_ortak.py` + `kunye_teyit.py`:** `ESAS_RE`/`KARAR_RE` nokta ZORUNLU tuttuğu ve `IGNORECASE` taşımadığı için `E:2020/1111`, `E: 2020/1111`, küçük harf `esas … karar …`, `E 2020/1111` biçimleri HİÇ görülmüyordu (kanıt: her biri ayrı koşuldu, kapı exit 0). Ayrıca AYM bireysel başvuru künyesi yapısal olarak görünmezdi — o usulde E./K. YOKTUR, künye `Başvuru Numarası: YYYY/N`dır (6216 s. Kanun m.45-49; MCP `mevzuat_icinde_ara` ile teyit; canlı AYM verisinde künyeler `BB 2015/53` biçiminde — MCP `aym_ictihat_ara` ile teyit). **Yapılan:** nokta opsiyonel + `:` ayracı + küçük harfli uzun biçim; `AYM_BB_RE` ve `AIHM_BASVURU_RE` desenleri; atıf sözlüğüne EKLEMELİ `kunye_turu` + `bas`/`son` alanları. **Daha önemlisi varsayılan TERSİNE çevrildi:** yeni `ayristirilamayan_atiflar()` — merci anılan bir SATIRDA karar iması (`2020/1111`, "sayılı/tarihli/günlü kararı") varken o satırdan hiç künye çıkarılamıyorsa sonuç "atıf yok — kapı AÇIK" değil, "mekanik teyit YAPILAMADI — kapı KAPALI"dır (fail-closed, `kunye_teyit` exit 1). Yanlış-BLOK üç katmanla engellendi (satırda zaten künye varsa atla · `DOSYA NO`/`ESAS NO`/`MERCİ` satırı muaf — Denizli 346 karnesinin tek parser yanlış-pozitifi · merci ile ima AYNI satırda olmalı). `KENDI_DOSYA_SATIR_RE` tek kaynağa (`kunye_ortak`) taşındı.
  - **B-3 (P0) — belgeye yalan yazma, `kaynakca_uret.py`:** blok, künyelerin teyit durumuna BAKMADAN "Aşağıdaki kararların tamamı tam metinleriyle okunup kütüğe damgalanmıştır" yazıyordu; kanıt koşusunda muhakeme kaydı HİÇ olmayan uydurma bir künye için bu cümle taslağa yazıldı ve DİSKE İŞLENDİ. **Yapılan:** beyan KOŞULLU — kütükte karşılığı olmayan tek bir künye varsa o cümle HİÇ yazılmaz; künye `⚠ TEYİT EDİLMEDİ` satırı alır (mevcut "⚠ erişim linki" şerhi LİNK yokluğuna dairdi, TEYİT yokluğuna değil). Üretecin kendi gevşek `ESAS_RE`/`KARAR_RE` kopyası silindi; çıkarım `kunye_ortak`tan gelir (tek kaynak: kapı neyi görüyorsa kaynakça da onu görür). Rapora/dönüş değerine `teyitsiz` sayacı eklendi.
  - **B-13 — mühür fail-open, `teslim_paketi.py`:** `artifact_sha256` alanı olmayan `.prov.json` sessizce "bayat" etiketiyle yutuluyordu. **Yapılan:** ayrı `shasiz` sınıfı + "mühür hiçbir şeyi bağlamıyor" gerekçeli açık RED. (Aynı gerçeğin `pipeline_kayit.py`'deki fail-OPEN kolu bu paketin dosyası DEĞİL — devir listesine yazıldı.)
  - **B-14 — `.docx` mühür kör noktası, `teslim_paketi.py`:** `.docx`/`.pdf` teslim sınıfı sayılıp 40-UYAP'a kopyalanıyordu ama filo-tazelik taraması yalnız `.udf`e bakıyordu; makbuz sonrası değişiklik penceresi bir uzantı için tamamen açıktı. **Yapılan:** `TESLIM_SINIFI_UZANTILAR` tek sabiti (`.udf/.pdf/.docx`); sha karşılaştırması uzantıdan bağımsız. MÜHÜRLÜ `.pdf/.docx`te bayat/sha'sız/okunamaz mühür RED'dir; MÜHÜRSÜZ `.pdf/.docx` advisory kalır (40-UYAP'a kopyalanan yoldaşlar tanım gereği mühürsüz doğar — RED saymak her yeşil teslimi bir sonraki koşuda kırardı). Mühürsüz `.udf` sözleşmesi v0.5.10'daki gibi korundu (regresyon testli).
  - **B-18 — girdi mutasyonu / idempotans kaybı, `kunye_ortak.py` + `dilekce_denetim.py` + `kunye_teyit.py`:** kaynakça bloğunun yazdığı `⚠` işaretini ikinci koşuda dilekçe denetiminin [C] OCR/alıntı teyidi yakalıyor, aynı komut aynı dosyada `rc=0 → rc=1 → rc=1` veriyordu. Kapı deterministik olmak zorundadır. **Yapılan:** `kunye_ortak.makine_blogu_maskele()` — işaretler arası bloğun YALNIZ üretece ait satırları (işaret/başlık/önsöz/`- ` maddeleri) aynı uzunlukta boşlukla değiştirilir (ofset ve satır numaraları korunur); üretici ile maskeleyici aynı sabitleri okur. Kötüye kullanım koruması: işaretler arasına ELLE yazılmış düz metin maskelenmez. Maskeleme `dilekce_denetim` (tüm kapılar, görünür [BİLGİ] notuyla) ve `kunye_teyit` girişine bağlandı.
  - **B-24 — kullanıcı hatası dosya kirletiyordu, `teslim_paketi.py`:** argümansız/hatalı çağrı CWD'de `_oa/defter/teslim-makbuz-RED.json` açıyor, orada sahte bir dava kökü işareti bırakıyordu. **Yapılan:** `_MAKBUZ_DURUM["arg_ayristi"]` — makbuz garantisi `parse_args()` BAŞARIYLA döndükten sonra devreye girer; ayrıca var olmayan `--kok` için makbuz dizini AÇILMAZ (görünür stderr notu). "RED bile damgalı makbuz keser" sözleşmesi KASITLIYDI ve korundu: argümanlar ayrıştıktan sonraki her başarısız yol RED makbuzu düşürmeye devam eder (regresyon testli).
  - **B-29 — ölü sarmalayıcı, `dilekce_denetim.py`:** depo genelinde 0 çağrılı `_t_beyan_var_mi` silindi ("bu kapı var" yanılsaması üretiyordu). `_index_yolu` (oa-pipeline) ve `_g2jdn` (oa-sure) bu paketin dosyaları değil — devre yazıldı.
  - **B-30 — `[K]` cephanelik bekçisi gerçek cümlede ateşlemiyordu, `dilekce_denetim.py`:** tek parça desendeki `[^.\n]{0,80}` penceresi hem MADDE ATFINDAKİ noktayı ("TBK m. 146") hem SATIR KIRIĞINI dışlıyordu; fiil listesi de dardı ("def'inde bulunabilir", "savunması muhtemeldir" görülmüyordu). Advisory'nin tek işlevi görünürlüktür, o da üretilmiyordu. **Yapılan:** tek dev regex yerine PENCERE MANTIĞI — metin cümlelere bölünür (RAKAMDAN sonraki nokta cümle sınırı DEĞİLDİR: "m. 146", "9. HD", "E. 2020/1111"; tek satır kırığı sınır değil, boş satır sınır), her cümlede fiil aranır ve fiilden ÖNCE aynı cümlede hasım sözcüğü şartı konur. Fiil listesi genişletildi. 6 ateşleme + 4 yanlış-pozitif vakası regresyona bağlandı; mevcut `test_v0581_tetikler.py` / `test_v059_hizli_kip.py` davranışı korundu.
  - **B-33 — makbuz şeması sürüklenmesi:** SKILL.md 11 alan sayarken üretici 17 alan yazıyordu. **Yapılan:** `references/cikti-semasi.md` §6 tek yetkili şema oldu (taban alanlar + yola özgü ek alanlar, makine okunur işaretler arasında); SKILL.md'deki alan listesi kaldırılıp belgeye bağlandı. İki test sürüklenmeyi kilitler: `_makbuz_taban()` anahtar kümesi ile belgelenen taban küme BİREBİR eşit olmalı, ve kaynaktaki her `ekstra={...}` anahtarı belgede yaşamalı.

## v0.5.16 — 2026-09-07 · İki Denetimin İnfazı (birleşim + entegrasyon)

> Kaynak: iki dış denetim raporu (2026-09-06, `_gorus/denetim-2026-09-06-*.md`), 15 sahiplik grubu (`v0516/<grup>` dalları), Yargı Pro hakem teyitleri (`_gorus/denetim-v0516-yargipro-teyit-<grup>.md`). Aşağıdaki blok(lar) ilgili grubun ajan raporundaki `changelog_notu` metninin aynıdır; entegratör notu birleşim sonrası hizalamayı (H1–H5) kaydeder.

### Grup C2 — kaynak rapor `19-C2-uygula.json`

## v0.5.16 — ATIF TANIYICI (K4) · AKIBET TÜKETİCİSİ (K5) · ÖRTÜŞME SAYACI (G11) · HÂKİM LENSİ (A-21/P2-7)

**Kapatılan bulgular:** K4 (Hamle 3, Kararlar #3/#4), K5 (Hamle 4), G11 (Hamle 11), A-21/P1-5, A-24/P2-7. Mevzuat MCP teyit 2026-09-06: HMK m.119/1-ğ, m.353/1-a, m.371.

- `scripts/kunye_ortak.py` — **K4:** `_YN` kurul esas biçimini («2020/9-111») tek sayı olarak korur (eskiden «2020/9» diye kesiliyordu). Yeni `BIRLESIK_EK_RE` + `_birlesik_ek_atiflari`: etiketsiz Yargıtay birleşik biçimi «2020/1111-2021/2222» ve «2019/4444-5555 sayılı» TAM künye (esas+karar, `bicim='birlesik'`) olarak ayrışır — yalnız merci anılan, kurul olmayan satırda (kurulda «YYYY/N-N» tek esas numarasıdır, bölünmez). `ayristirilamayan_atiflar` sözlüğüne EKLEMELİ `sinif` alanı: `SINIF_EKSIK_KUNYE` (tarih-only «12.03.2019 tarihli kararı/ilamı», K-only «K. 2021/2222 sayılı», E-only, etiketsiz tek sayı) ve `SINIF_AYRISTIRILAMAYAN`. Esas-only/karar-only ayrışan satır artık 'dolu' sayılmaz (kütükte tek sayı eşleşmesiyle TEYİTLİ geçebilen fail-open kapandı). «tarihli/günlü» yalnız «karar/ilam» ile bitişikse imadır — «28.02.2018 tarihli Resmî Gazete», «05.12.2024 tarihli ihtarname» BLOK değildir; `KENDI_DOSYA_SATIR_RE` istisnası korunur. Yeni `_pencere_basi`: merci geri-bakış penceresi satır başını aşmaz (ardışık satırlardaki iki künyenin mercisi/spanı karışmaz — saha regresyonu). **K5:** `kutukten_son_akibet` (+ ortak `_kutukten_son_token`; `kutukten_son_damga` davranışı bit düzeyinde aynı).
- `scripts/kunye_teyit.py` — EKSİK KÜNYE sınıfı `[TEYİTSİZ]` etiketi ve sınıf adıyla raporlanır → exit 1; ipucu metni birleşik biçimi gösterir.
- `scripts/ictihat_muhakeme_denetim.py` — **G2 EKSİK KÜNYE** (`eksik_kunye_bloklari`): tarih-only/K-only/E-only atıf muhakeme kaydıyla eşleşemeyeceğinden çıplak atıf gibi BLOK («esas+karar tamamlanmalı (Yargıtay birleşik biçimi kullanır, tanıyıcı artık görür)»); makine kaynakça bloğu B-18 maskesiyle bu scriptte de görülmez. **[G5-AKIBET]** (`akibet_denetimi(atiflar, kayitlar, kutuk)`): `MuhakemeKaydi.akibet/akibet_kaynak` («**AKIBET:**» / «**AKIBET-KAYNAK:**» satırları, enum `kesinlesti|kesinlesmedi|bozuldu|kaldirildi|geri_cevrildi`, `_akibet_normalize` Türkçe yazımı katlar) + kütük «AKIBET=» tokenı (esas; çelişkide görünür uyarı). DAMGA=LEHE ∧ AKIBET∈{bozuldu,kaldirildi} ∧ dilekçede atıf → BLOK («bozulmuş/kaldırılmış karar LEHE dayanak olamaz» — HMK m.371 / m.353/1-a); LEHE ∧ kesinlesmedi → UYARI («kesinleşmemiş — akıbet izlenmeli, kanun yolu açık»); LEHE ∧ geri_cevrildi → UYARI; ALEYHE ∧ bozuldu → UYARI (cephanelik); AKIBET yoksa kapı ateşlemez (geriye uyum); enum dışı değer görünür uyarı, BLOK değil. Rapor bölümü `[G5-AKIBET]`, exit sözleşmesi G5 ile aynı. **G11:** `ortusme_zenginligi_uyarisi` «(1)…; (2)…; (3)…» parantezli iç sıralamayı ve «;» ayraçlı parçaları sayar — tek paragrafa yazılmış üç somut nokta «yüzeysel» uyarısı almaz, tek cümle yine alır (advisory).
- `SKILL.md` — MEKANİK KAPILAR: K4 «EKSİK KÜNYE» sınıfı, G2 EKSİK KÜNYE / [G5-AKIBET] / G11 notu. B listesi: **«İLK SAYFA + UZUNLUK testi»** (P2-7/A-24; bölüm sayısı ve KB görünür raporlanır, eşik SAYI DEĞİL BANT; ilk sayfa tek başına tezi taşıyor mu — lens, kapı değil). C2 hakem brifine **İKİNCİ PAS — HÂKİM LENSİ** (A-21/P1-5): on dakika testi, talep bloğu tek başına yeterli mi (HMK m.119/1-ğ), gerekçe alışkanlığı, daire eğilimi (kütüğe dayanmadan yazılmaz), iş yükü — lens, kapı değil.
- Testler: `tests/test_v0516_C2.py` (53). Mevcut oa-kontrol testleri değişmeden yeşil. `tests/README.md` OA-SUIT-SAYISI güncellendi (B-35).

Saha dersi: tarih tek başına künye değildir (aynı gün aynı daire onlarca karar verir); tek sayı tek başına künye değildir (esas/karar her dairede yılda sıfırdan başlar) — «atıf yok» sanılan bu iki biçim artık «EKSİK KÜNYE»dir ve kapıyı kapatır; künye ile mercisi aynı satırda anılır, pencere satırı aşmaz.

### Grup C2 — kaynak rapor `27-C2-uygula.json`

## v0.5.16 — oa-kontrol onarım turu (hakem bulguları, 2026-09-07)

- **kunye_ortak.py — K4 REGRESYON kapatıldı (KRİTİK):** `_YN` kurul tire soneki `(?:-\d{1,6}(?!\s*/))?` lookahead geri izlemeyle kısmi eşleşiyor, «E. 2020/1111-2021/2222 [K.]» girdisinde esas «2020/1111-202» (bozuk) + karar kaybı + sahte EKSİK KÜNYE üretiyordu (taban 80ac847 TAM künye veriyordu). Düzeltme `(?:-\d{1,6}\b(?!\s*/))?` — kelime sınırı geri izlemeyi keser; kurul «2020/9-111» korunur. Etiketli birleşik biçim tabanla aynı kanonik değere döner.
- **kunye_ortak.py — etiketli birleşik yükseltmesi:** `_ETIKETLI_BIRLESIK_KUYRUK_RE` + `esas_karar_atiflari`: K. etiketsiz «E. 2020/1111-2021/2222 sayılı» esas+karar'a tamamlanır (`bicim='birlesik'`); aynı-yıl «E. 2019/4444-5555» satırda kurul yok ve numaralı daire varsa bölünür, belirsizse bütün kalır (E-only → EKSİK KÜNYE, fail-closed).
- **kunye_ortak.py — yanlış-BLOK yasağı (tarih-only):** `_tarih_merciye_ait` / `_gecerli_karar_imasi`: tarih-only iması ancak merci tarihten önce anılmış ve arada mahkeme iyeliği («yerel mahkemenin», «ilk derece mahkemesinin», «hâkimliğinin») yoksa geçerli. «Yerel mahkemenin 05.06.2024 tarihli kararı Yargıtay'ın yerleşik içtihadına aykırıdır» artık `kunye_teyit` ve `ictihat_muhakeme_denetim` G2'de BLOK değil (saha dersi: istinaf/temyiz dilekçesinin en sık cümlesi; tarih kendi dosyasının kararına aittir).
- **ictihat_muhakeme_denetim.py — [G5-AKIBET] kütük normalize:** `akibet_denetimi` kütükten okunan `AKIBET=` tokenını da `_akibet_normalize`'dan geçirir; D grubu Türkçe harfli/tireli yazsa da enum eşleşir, kayıt/kütük çapraz kontrolü aynı normalizasyonla.
- **SKILL.md:** G2 kapsam notu (genel AYRIŞTIRILAMAYAN sınıfı da BLOK — B-2 simetrisi), etiketli birleşik/aynı-yıl kuralı, yerel-mahkeme tarih istisnası, K5 normalize.
- **tests/test_v0516_C2.py:** +12 onarım testi (65 toplam).

### Entegratör notu (2026-09-07)

- **Entegrasyon (H4):** D (`oa_hafiza.py teyit --akibet`) → C2 (`ictihat_muhakeme_denetim.py` [G5-AKIBET]) uçtan uca test: LEHE+bozuldu+atıf → BLOK; kesinleşti → geçer; akıbetsiz eski kayıt kapıyı ateşlemez. Cephanelik ürün deseni `06-antitez*` (≤v0.5.15 `07-antitez*` kabul).

## v0.5.16.1 — saha yan-bulgusu: kütük ayrıştırıcısı SÜTUN TOLERANSI (2026-09-07)

> Kaynak: v0.5.16'nın gerçek dava kopyalarında saha ölçümü — 26/33 taslakta her künye «damgasız» görünüyor, [F] hafif kip ve kütük teyidi kör. Teşhis: kütük okuyucuları satırı `split("|")` ile bölüp `len != 7` ise «BOZUK, fail-CLOSED atlandı» diyordu; saha kütüklerinde satırlar **17 hücreli** (genişletilmiş düzen). Ölçüm: `oa_hafiza.py teyit` 5 sütun = 7 hücre yazar (v0.5.0'dan beri değişmedi) → 17 hücre yazıcı dışı/genişletilmiş düzendir; okuyucu sütun sayısına bağlı kalamaz. Dal `v0516/yan-bulgular`.

- `scripts/kunye_ortak.py` — yeni tek giriş kapısı **`kutuk_veri_satirlari(kutuk_yolu, etiket)`** (+ `kutuk_satiri_veri_mi`, `kutuk_satir_hucreleri`): VERİ satırı = `|` ile başlayan, en az bir hücre kurabilen, başlık («Zaman») ya da ayraç (`|---|`) olmayan satır; `|` ile başlamayan düz metin sessiz atlanır; **BOZUK** yalnız tek hücre bile kuramayan (kapanmamış `|`) satır — görünür uyarı, süreç başına bir kez. **`kutuk_satiri_kunyeyle_eslesir(satir, esas, karar, daire)`**: esas+karar satırın TAMAMINDA (`sayi_var`), esas VE karar biliniyorsa ikisi de şart (v0.5.5 fail-open kapatması korunur); daire **`kutuk_satiri_dairesi`** ile künyeyi TAŞIYAN hücreden, yoksa satırdaki tek daireden okunur (daire-kör değil; sorgu hücresindeki daire künyenin dairesi sanılmaz). **`kutuk_satiri_kunyesi(satir)`**: DAMGA/AKIBET taşıyan tam künye hücresi öncelikli. `kutukten_son_damga` / `kutukten_son_akibet` (ortak `_kutukten_son_token`): token satırın tamamında, **birden çok `DAMGA=` → sonuncusu** (satır içi ve satırlar arası). `kutuk_gercek_veri_var_mi`, `kutukte_esas_karar_satiri_var_mi`, `kutukte_damgali_dayanak_satiri_var_mi` (kaynak-izi satırın herhangi bir hücresinde) aynı kapıdan geçer. Eski 5-sütun/7-hücre satırlar bit düzeyinde aynı okunur.
- `scripts/ictihat_muhakeme_denetim.py` — `_kutuk_kunye_bilgisi` ([G6] tam-metin/duyulmuş) ve `farkindalik_denetimi` (ALEYHE → cephanelik) yerel 7-hücre aynalamasından çıkıp `kunye_ortak` kapısına bağlandı (tek-yazar kuralı).
- `scripts/kunye_teyit.py` — `kutuk_ham_baglari`: Döküm bağı hücre konumundan bağımsız (`ham/` segmenti süzer).
- **Yazıcı simetrisi (oa-pipeline `oa_hafiza.py`):** satır-geneli aramanın açtığı kapı — kullanıcı-kontrolündeki `--sorgu` hücresinden `DAMGA=` / yabancı künye izi enjekte etme — yazıcıda kapandı: sorgu hücresi de `_sonuc_damga_ize_karismasin` katmanından geçer ve «YYYY/N» künye izi görsel eşdeğerle (`⁄`) nötrleştirilir (`_sorgu_kutuk_ize_karismasin`; triyaj-ice-al dahil). Kütükte kalan tek gerçek `DAMGA=`/künye izi script'in doğruladığı sonuc hücresininkidir.
- **Etki:** oa-dilekce `hizli_denetim` [F] hafif kip (kod değişmedi) 17 hücreli kütükle «ALEYHE künye» satırı üretir; `oa_hafiza teyit` yazma-öncesi DAMGA çapraz kontrolü ve `--damga-degistir` ritüeli saha kütüğünde yeniden çalışır.
- `SKILL.md` — MEKANİK KAPILAR (iv) notu.
- Testler: `tests/test_v0516_yan_bulgular.py` (17; bölüm 1: gerçek `teyit` satırı ALEYHE/AKIBET/LEHE, 17 hücre, daire farkı, 7 hücre geriye uyum, son-DAMGA semantiği, BOZUK yalnız gerçek bozukta, [F] hafif kip in-process, `ictihat_muhakeme_denetim`/`kunye_teyit` okuyucuları, `--sorgu` enjeksiyon kilidi). Komşu süit (kunye_teyit, ictihat_muhakeme_*, v0516 B/C1/C2/D, pipeline_kayit_onkosul + 14 dosya) 615/615 yeşil; `tests/README.md` OA-SUIT-SAYISI 2318.

- **Bilinçli karakterizasyon değişikliği — `tests/test_kaynak_url_tutarlilik.py` fikstürü:** [G4] testleri kütüğe 6 sütunlu el yazısı satır (`| … | E. … K. … | LEHE | dokum |`) yazıyordu; v0.5.16 okuyucusu onu `len != 7` diye YOK sayıp kütüğü «fiilen kullanılmıyor» ilan ediyor, G3 kütük-dayanak ve [G6] tam-okuma denetimlerini SESSİZCE atlıyordu (fail-open — tam da saha kusurunun test içindeki izdüşümü). Toleranslı okuyucu satırı görür ve dayanak ister; fikstür kanonik satıra (`DAMGA=LEHE DOKUM-SINIFI=tam-metin` + `[döküm](_oa/teyit/dokum/kaynak.md)`) çekildi — testin konusu ([G4] KAYNAK-URL) değişmedi. Test süitinde başka 7-hücre-dışı el yazısı kütük satırı yok (tarandı).

Saha dersi: «BOZUK» teşhisi görünürdü ama yanlıştı — satır bozuk değil, düzen genişti; sessiz atlama yasağı yalnız uyarı basmak değil, doğru teşhis koymaktır. Okuyucu biçime değil içeriğe (künye + token) bağlanır.
- **2026-09-10 (Yargı Pro ile backend testi — B8):** `ictihat_muhakeme_denetim.py` [G2] kapısı, dilekçenin **KENDİ künye bloğundaki dosya numarasını** çıplak içtihat atfı sayıp TESLİM ENGELİ üretiyordu. Bu, CHANGELOG'da anılan **346 sahasının tek ayrıştırıcı yanlış-pozitifi**dir ve o gün *«yeşil makbuzu imkânsız kıldı»* diye kayda geçmişti; muafiyet yazıldı ama yalnız İKİ yere: `kunye_ortak.ayristirilamayan_atiflar` ve `kunye_teyit.kendi_dosya_no_ayikla`. **Kardeş kapı olan bu script muafiyeti hiç taşımıyordu** — `KENDI_DOSYA_SATIR_RE`'nin başındaki «TEK KAYNAK burasıdır; kunye_teyit.py bu tanımı kullanır» notu tek kaynağı ilan etmiş ama tek TÜKETİCİ varsaymıştı. Onarım: muafiyet gerçekten tek kaynağa taşındı (`kunye_ortak.kendi_dosya_no_mu()` + `satir_metni_no()`) ve `taslaktaki_atiflari_bul()`'a bağlandı. Muafiyet DAR ve fail-closed — dört şart BİRDEN: (1) özel künye türü değil (AYM bireysel başvuru/AİHM gerçek atıftır, karar no'ları 6216 m.45-49 gereği zaten yoktur), (2) E.+K. çifti tam değil, (3) daire/merci anılmıyor, (4) satır DOSYA NO/ESAS NO/MERCİ etiketiyle başlıyor. **Neden bu kırık önemliydi:** yanlış-pozitif bir kapı müvekkile doğrudan zarar vermez ama sistemin bütün mimarisi «kapıya güven» üzerine kuruludur; geçerli dilekçesi düzenli olarak sahte engelle karşılaşan avukat kapıyı `--serh` ile geçmeye ALIŞIR ve o alışkanlık sıra GERÇEK çıplak künyeye geldiğinde de işler — yanlış-pozitif, kapıyı doğrudan değil ona duyulan güveni aşındırarak açar. Onarımın kapıyı gevşetmediği ayrıca ölçüldü: gerçek çıplak künye BLOK · DOSYA NO satırındaki TAM künye (daire+E+K) BLOK · ALEYHE damgalı künye BLOK (anayasa m.6) · geçerli LEHE künye exit 0. Bulgu, Yargı Pro'dan çekilen GERÇEK kararlarla kurulan uçtan uca teyit-zinciri testinde ortaya çıktı (sentetik veri bunu göremiyordu); aynı testte künye ayrıştırıcı 10 farklı merci biçiminde 10/10, halüsinasyon kapısı (verbatim doğrulama) uydurma alıntıyı RET, damgasız/kısa-bağlı kayıtlar RET verdi. 6 yeni kilit (`tests/test_v0517_muvekkil_lehine_kiriklar.py` B8 bloğu); onarım geri alındığında 4 test kırmızı yanıyor. Rapor: `_gorus/denetim-2026-09-10-backend-muvekkil-lehine-kiriklar.md` §B8.
