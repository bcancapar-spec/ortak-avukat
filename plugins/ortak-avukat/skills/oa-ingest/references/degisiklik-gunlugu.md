# oa-ingest — Değişiklik Günlüğü

(SKILL.md gövdesinden bağlam ekonomisi için ayrıldı; içerik aynen korunur.)

- **2026-07 (v1.0):** İlk sürüm (Can yönlendirmesi — UYAP evrak indiricisinin "insan gözü" çıktısı yapay zekâda milyonlarca token yakıyor; 0. MANİFEST'in AI katmanı eksikti). Deterministik çıkarım motoru: metin PDF→PyMuPDF (bedava/kayıpsız), taranmış PDF→render+OCR, UDF→content.xml, EYP/.zip→aç, TIFF/JPG→OCR (çok sayfa), DOCX→document.xml. "Metin mi tarama mı" ölçümle (sayfa başına anlamlı karakter eşiği) belirlenir. Çıktı: belge-başına `_oa/metin/NNN-*.md` + `00-kunye.json` + `00-INDEX.md`; her metin kaynağına bağlı, OCR "⚠ teyit" damgalı, orijinal salt-okunur. Gerçek dava dosyalarında test edildi (Antalya idari: metin PDF'ler kayıpsız, taranmış PDF/TIFF/JPG OCR; Denizli iş: birleşik PDF'ten 159K karakter bedava, 39MB salt-görüntü ⚠ işaretlendi). Windows-dostu: `pip install pymupdf pillow` (binary'siz), OCR için Tesseract+`tur` opsiyonel.
- **2026-07 (v1.0):** Çaba/token düsturu GÜNCELLENDİ (Can yönlendirmesi — "tasarruf da artık hedeftir; muhakemede değil mekanik/temsil katmanında, veri-kayıpsız"): eski "tasarruf hedef değildir" ilkesi, "verimlilik bir hedeftir ama yalnız mekanik katmanda ve kayıpsız; muhakeme/doğrulama/derinlik asla kısılmaz" biçiminde yeniden yazıldı. Bu parça ilkenin saf uygulamasıdır. Aynı düstur tüm aileye işlendi.
- **2026-07 (v1.5):** PARALEL ÇIKARIM (Fable K reçetesi — büyük külliyatta duvar-saati kısaltma): FAZ B (saf çıkarım) `ProcessPoolExecutor` ile çok-çekirdekli koşabiliyor; `--isci N` (0=otomatik `min(çekirdek,8)`, 1=seri). Determinizm ÖDÜN VERİLMEDEN: künye (`00-kunye.json`) ve md sha256'ları seri/paralel arasında BYTE-ÖZDEŞ (arşiv-içi kimlik göreli-yol tabanlı, tek-yazar ebeveyn birleştirme SIRALI-İNDEKS, işçi çökmesi sessizce yutulmaz — `tests/test_oa_ingest_paralel.py` kanıtı). Ölçek doğrulaması: 67 evraklı sentetik korpusta `--isci 1` ~21sn ↔ `--isci 8` ~6sn (~3.5x), çıktı byte-özdeş (bkz. proje hafızası `asama-2-olcek-dogrulama.md`).
- **2026-07 (Aşama 4 — pipeline entegrasyonu):** `oa-pipeline` 0. MANİFEST hattı (SKILL.md sabit hat + EVRAK ATLAMA YASAĞI + SUBAGENT fan-out) `oa_ingest.py --isci` paralelliğini belgeledi; bu parçanın "İş akışı (pipeline adım 0)" adım 2'si `--isci` varsayılan-otomatik notunu ve mutabakat (`manifest_olustur.py --mutabakat`) ile ilişkisini açıkça yazdı. Kod tarafında değişiklik yok (v1.5 zaten paralel + determinist); değişen yalnız dokümantasyon bağlantısıdır. Entegrasyon testi: `tests/test_pipeline_ingest_entegrasyon.py`.
- **2026-07 (v1.5.1 — Fable backlog sağlamlaştırma, Aşama 3):** (a) Arıza sonucu ({hata, atlandı}) artık önbelleğe YAZILMAZ — araç (Tesseract/PyMuPDF) sonradan kurulunca veya dosya onarılınca imza (mtime+size) aynı kalsa bile bayat "YÜKLENEMEDİ" önbellekten basılmaz; her koşuda yeniden denenir. 'zaman-aşımı' kasıtlı İSTİSNA: OCR zaman-aşımı pahalı olduğu için önbellekte kalabilir (`--yeniden` ile açıkça atlanır). (b) `main()` başında hedef dizindeki önceki çökmüş koşulardan kalan `.tmp-oaing-*.part` atomik-yazım artıkları süpürülür. (c) FAZ A/C sonrası, önbellekte olup diskte artık OLMAYAN (silinmiş) kaynakların önbellek kaydı budanır — hâlâ diskte duran ama o koşuda sırası gelmemiş kaynaklar dokunulmaz (FAZ A her koşuda TÜM klasörü tarar). (d) POISON-EVRAK İZOLE YENİDEN DENEME: havuzda bir işçiyi gerçekten öldüren "zehirli" evrak artık aynı batch'teki masum evrakları kalıcı kaybettirmiyor — FAZ C'den önce çöken kalemler tek-işçi izole havuzda birer birer yeniden denenir; yalnız izole halde de çöken evrak nihai "işçi çöktü" damgasını alır. (e) `_ADMIN` kovasına 'atlandı' ve 'zaman-asimi' eklendi: OCR hiç yapılmamış (atlanmış/zaman-aşımına uğramış) kayıtlar artık `ocr_teyit_gerek` sayacına girmiyor — bu, `00-kunye.json` byte-çıktısını etkiler; mevcut testler buna göre güncellendi. Yeni testler: `tests/test_oa_ingest_paralel.py` (8) arıza önbelleksiz, (9) önbellek budama, (10) poison-evrak izole retry. Tüm paket (107 test) + aile denetimi yeşil.
- **2026-07 (v1.6 — GATE A+C, M1-2 paketi, Denizli canlı testinden):** (A) Sayfa/bölüm haritası: `karakter > --buyuk-esik` (varsayılan 40.000 anlamlı karakter) olan her evrak için md YANINA `<taban>.harita.json` üretilir — mevcut `<!-- --- sayfa N --- -->` ayracından offset (üretilen .md'deki karakter konumu) + ilk-satır(başlık) + karakter/token; ÖZETLEME YOK, yalnız metnin kendisinden türetilen deterministik yapısal bölme (ayraç yoksa tüm gövde tek 'bölüm'). Künyeye `buyuk`/`harita` alanları eklendi; `00-INDEX.md`'ye 'büyük' özet sayacı + Harita sütunu eklendi. (C) Mekanik 'tur~' tahmini: dosya adı/temiz addan (içerik OKUMADAN) tebligat/karar/dilekce/bilirkişi/sicil/bilanço/vekâletname/harç-makbuz/istinabe/duruşma-tutanağı gibi bir tür tahmin edilir (sıralı liste, ilk eşleşen kazanır → deterministik); eşleşme yoksa `null` (uydurulmuş varsayılan YOK). Künyeye `tur_tahmini` alanı eklendi; `00-INDEX.md`'de daima "<tür> (tahmini)" damgasıyla — advisory, kesinlik değildir. Her iki kapı da SAF (yalnız ebeveyn tek-yazar aşamasında, mevcut metin/ad/kaynak'tan türer) → seri==paralel byte-eşitliği ve idempotens ETKİLENMEDİ. Künye şeması genişledi (`buyuk`, `harita`, `tur_tahmini`, üst seviyede `buyuk_evrak`/`buyuk_esik`) — geriye dönük uyumlu (ek anahtar, mevcut okuyucular etkilenmez); şema değişikliği BİLİNÇLİ BEYAN edilir (bkz. proje hafızası `paket-m1-2-gate-ac-ingest.md`). Yeni testler: `tests/test_oa_ingest_gate_ac.py` (6 test: küçük evrak bayraksız, duz-metin bölüm haritası, PDF çok-sayfalı sayfa haritası, seri==paralel yapısal özdeşlik, tür~ eşleşme, arızalı kayıtta da tür~ çalışır). Mevcut paralel/entegrasyon testleri (dinamik yapısal karşılaştırma yapan `test_oa_ingest_paralel.py` ve `test_pipeline_ingest_entegrasyon.py`) DEĞİŞTİRİLMEDEN yeşil kaldı — sabit/golden bir fixture dosyası yoktu, karşılaştırmalar hep aynı-koşu dinamik oldu. Tüm paket (150 test) + aile denetimi yeşil.
- **2026-07 (v3.22 — M2-3):** Sürüm işaretçisi ailenin ortak M2-3 entegrasyon sürümüne hizalandı (`aile_dogrula.py` sürüm tutarlılık uyarısını temizlemek için); bu satırın kendisi dışında bu parçada işlevsel bir değişiklik YOKTUR — gerçek içerik değişiklikleri (varsa) yukarıdaki ayrı kayıtlardadır.
